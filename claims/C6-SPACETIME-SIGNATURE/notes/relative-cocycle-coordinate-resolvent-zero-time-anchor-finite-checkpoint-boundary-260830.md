# R-386 coordinate-resolvent zero-time anchor boundary

R-386 / EXP-001229 is a claim-nonbearing finite checkpoint for the
relative-modular cocycle route.  For each actual Q3 bond prefix on the V=2
edge and V=4 square, the boundary addition is a position-only polynomial
`B(q_x,q_y)`.  The coordinate seed `A_z=(i eta I-q_s)^(-1)` therefore obeys
`[B,A_z]=0` (and the same for `A_z^*`) in the bounded tensor truncation.

The primary lane passes 2025/2025 assertions, the non-importing independent
lane 2022/2022, the integrated verifier 51/51, and Lean R386.  The grid has
288 beta-weighted contexts, 144 seed rows, 10 bond prefixes and 1152 dynamic
rows.  The primary maxima are `2.317703490729531e-16` for the zero commutator,
`1.259956570249438e-10` for the first variation,
`6.238635588734805e-07` for the second finite-difference comparison,
`4.232440506926493e-15` for the second-variation reduction, and
`3.6054124684301573e-10` for the modular first variation.  The finite dynamic
norm divided by `t^2` reaches `0.6576383064349549`.

The exact finite implication is only that the relative-dynamics difference
has zero first variation and a reduced second nested commutator at time zero.
It does not provide a uniform quadratic remainder, an invariant unbounded
domain, a phase-local BKM estimate, shell `l1` summability, direct `D` or
`delta-D` Cauchy convergence, common alpha, OS/KMS/GNS identification, a gap,
continuum, C6, Sector-A or Pre-A.  A hostile `+(1/4)p_s` mutation is rejected
with minimum commutator residual `0.279128784747792`, confirming that the
anchor is tied to the position-only bond class.  No negative result, tier
change or PDF is issued.
