# R-414 — two-scale semigroup bridge

R-414 records a finite, claim-nonbearing reorganization of the R-413
Mellin bridge.  For positive ordered modes, the interval `[0,tau]` is bounded
by a sublinear UV heat power and the interval `[tau,infinity)` by the first
positive spectral gap.  The resulting finite criterion is

```
tr(W^+) <= A_tau tau^(1-alpha)/(1-alpha) + H(tau)/gamma,
```

where `A_tau = r tau^alpha + C_UV alpha Gamma(alpha)`,
`gamma = lambda_1`, and `H(tau)` is the positive heat trace at the split.
The first term is an ultraviolet proper-time budget; the second is an
infrared semigroup budget.

Primary and independent lanes cover the complete R-399 conditional-row grid
and all R-412 mixed profiles.  The primary lane passes `191471/191471`, the
independent lane passes `191470/191470`, the hostile lane passes `9/9`, the
integrated verifier passes `42/42`, and Lean R414 compiles.  The minimum
finite Green-bound slack is `2.936364851149518` and the maximum Mellin
identity residual is `3.885780586188048e-16`.

This is a precise uniformity target rather than a uniform theorem.  The
repository still lacks cutoff/volume/source/phase/exhaustion-uniform control
of `A_tau`, `gamma`, and `H(tau)` on a common Hamiltonian core.  R-399 shell
transfer, R-406 Schur residual control, OS/KMS/GNS reconstruction, the
physical gap, continuum, C6, Sector-A and Pre-A remain open.

The adversarial lane rejects IR-head or UV-term omission, use of the largest
instead of smallest eigenvalue, omission or wrong shifting of the late
budget, reversed time ordering, a wrong Mellin sign, and `alpha=1`.

No tier change, negative result or PDF is issued at this finite checkpoint.
