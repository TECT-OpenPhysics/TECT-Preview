# R-385 finite relative-modular cocycle checkpoint boundary

R-385 / EXP-001228 is a claim-nonbearing finite checkpoint for the Q3LOCK
common-dynamics route.  On the declared V=2 edge and V=4 square oscillator
truncations, the ordered relative product
`U(t)=exp(+i t H_prime) exp(-i t H)` was checked for every actual term prefix,
both term orders, both time signs, both beta values, all translated sites,
both resolvent imaginary parts, all composition pairs and both adjoint seeds.

The primary lane passed 3664/3664 assertions, the non-importing independent
lane passed 608/608 aggregate assertions, the integrated verifier passed
87/87, and Lean R385 compiled.  Maximum residuals were `2.8751206244079863e-14`
for relative intertwining, `1.3568796804866375e-14` for cocycle composition,
`2.850589646690371e-10` for the finite-difference derivative,
`1.708982179746617e-16` for the resolvent identity, and
`1.0872551803121098e-14` for unitarity.  The weighted commutator maximum was
`0.7980860666930905`; it is a finite diagnostic, not a smallness estimate.
The hostile reversed orientation produced residual `0.12803849472459028` and
was rejected above the `1.0e-7` threshold.

The finite result validates only ordered algebra, finite derivative and
resolvent bookkeeping, and the declared two-sided Gibbs-weighted rows.  It
does not prove a phase-local BKM estimate, boundary-shell `l1` summability,
cutoff/source/volume/shape uniformity, an invariant operator domain, direct
`D` or `delta-D` Cauchy convergence, a common alpha, Hamiltonian-to-OS/KMS
identification, a gap, continuum, C6, Sector-A or Pre-A.  These remain named
open gates.  No negative result, tier change or PDF is issued.
