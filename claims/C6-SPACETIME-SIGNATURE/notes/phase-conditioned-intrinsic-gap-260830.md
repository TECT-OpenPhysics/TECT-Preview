# R-405 - phase-conditioned intrinsic gap and capacity split

R-405 / EXP-001250 is a T0 claim-nonbearing finite checkpoint following
R-404.  It separates the momentum-weighted intrinsic graph into induced
negative/positive q-coordinate sector gaps, cross-sector conductance and
neutral central mass.

The primary lane covers eight finite Q3 Gibbs systems (volume two dimensions
`4,5,6,8,10,12`; volume three dimensions `4,5`), beta
`{1/2,1,2,4,8}`, both collar orientations, all prefix conditional rows,
and passes `1178/1178` over `80` profiles and `1030` rows.  The independent
lane passes `1088/1088`, the hostile lane `5/5`, the integrated verifier
`33/33`, and Lean R405 compiles.

The full intrinsic gap ranges from `0.6310329497027756` to
`6.229495058532403`; the smaller induced sector gap ranges from
`1.5773625260965005` to `20.555978652063708`; cross-sector capacity ranges
from `0.11650039514772156` to `1.222041249326006`; the minimum mass in either
sector is `0.010133814803601744`; and neutral central mass is at most
`0.3870415227365262`.  Across beta `1/2` to `8`, the minimum full gap falls
from `1.0022812277674598` to `0.6310329497027756`, while the sector floor stays
near `1.58`.

The finite separation suggests a phasewise route: the global graph may contain
a softer phase-switching mode than either within-phase form.  This is only a
finite diagnostic.  It proves no phase selection, cutoff/volume/source/
exhaustion-uniform lower bound, common core, common alpha, OS/KMS/GNS gap,
continuum, C6, Sector-A or Pre-A closure.

The next gate is an analytic phase-conditioned sector Poincare estimate with
explicit control of the neutral level and cross-sector capacity, followed by
its justified transfer to the R-399 shell.
