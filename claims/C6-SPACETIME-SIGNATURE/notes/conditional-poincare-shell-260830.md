# R-399 — Conditional Poincare-before-Duhamel shell transport

R-399 adds a new finite route interface after the R-398 Doob shell identity.
For every oriented collar prefix, the shell increment is a conditional
variance of the coordinate likelihood.  On the ordered finite coordinate
levels, a reversible birth-death form with conductance
`min(pi(k),pi(k+1))` gives a computable first nonzero gap `lambda`.  The
conditional Poincare inequality converts the shell variance to a discrete
conditional gradient budget divided by `lambda`.

The complete finite actual-Q3 grid has five volume/cutoff systems and 3,584
oriented history contexts.  Primary and independent lanes agree, hostile
mutations catch omission of the gap denominator and the conditional parent,
the integrated verifier passes 31/31, and Lean R399 compiles.  The observed
minimum finite conditional gap is `0.49325229280535315`; the largest weighted
Poincare shell bound is `0.00045782017814794144`.

This is claim-nonbearing T0 evidence.  The finite minimum gap is not a
cutoff/volume/source/phase-uniform lower bound, and the discrete gradient is
not a real-time Lieb--Robinson estimate.  The next gate is a
phase-conditioned uniform conditional gap plus uniform gradient decay on a
Hamiltonian-derived common core.  Common alpha, OS/KMS/GNS, a mass gap,
continuum, C6, Sector-A and Pre-A remain open.

**Authority:** [R-399 certificate](../../strategy/pre-a-cp1-st8-q3lock-conditional-poincare-shell-certificate-260830.md), [primary run](../runs/2026-08-30-primary-pre_a_cp1_st8_q3lock_conditional_poincare_shell/primary.json), [integrated run](../runs/2026-08-30-integrated-pre_a_cp1_st8_q3lock_conditional_poincare_shell/integrated.json), and [Lean R399](../../verification/lean/Tect/R399.lean).
