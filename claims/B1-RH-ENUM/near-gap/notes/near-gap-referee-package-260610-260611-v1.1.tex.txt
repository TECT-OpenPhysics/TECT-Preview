% =============================================================================
% Title: Referee package (DRAFT) -- near-gap residual closure with explicit margins, and the self-caught $O(I^2)$ correction
% Claim: B1-RH-ENUM (referee package; pending operator confirmation)
% Version: v1.1 -- first issued 2026-06-10; this version issued 2026-06-11
% Status: AUXILIARY (not a main-line referee artefact; cited by the main line, DRAFT
%   bundle only, NOT promoted to PUBLISHED -- main-proof-line.md, operator-confirmed 2026-06-11).
%   REFEREE PACKAGE DRAFT -- DRAFT ACCEPTED (operator 2026-06-11), published-bundle confirmation HELD pending reproduction-bundle inspection. Folder result: convention-exactness closure + a self-caught retraction (honest record). Self-contained
%   referee-facing assembly for external review; pending the operator-confirmation gate
%   (governance/reproduction-bundle-policy.md sec.11) before a PUBLISHED bundle is built.
%   Reproduction: 1 script(s) in this folder's bundle. Grounded by the folder's notes.
%
% Revision history (cumulative):
%   v1.0 (2026-06-10) first DRAFT issue for operator review. Synthesises the folder's
%        headline result into a self-contained referee document. Awaiting operator confirm.
%   v1.1 (2026-06-11) operator review: DRAFT ACCEPTED, published-bundle confirmation HELD
%        pending 1-script reproduction-bundle inspection. Patch: added the retracted-vs-
%        corrected table and the closure-margin clarification sentence (Sec.2).
% =============================================================================

\sloppy
\section*{1. Purpose and scope}

This referee document concerns the NEAR-GAP residuals of B1-RH-ENUM: convention exactness with explicit margins near the Brazovskii gap, and the honest retraction of an earlier $O(I^2)$ over-claim. \textbf{Scope:} the near-gap convention and the common-mode repair. \textbf{Not claimed:} the superseded $O(I^2)$ numbers are withdrawn (recorded below).

\section*{2. The result}

(i) The earlier R-U10-1 $O(I^2)$ numerical claims are SUPERSEDED by the $u$-series triage: the remainder is $1.65\times10^{-3}$ (not $2.7\times10^{-5}$), the protection is $\times2$ (not $\times130$), and the identity is $M'=-J(0)/2$ (not $-J(0)$). (ii) With the corrected convention, the near-gap residual closes with explicit margins via the common-mode repair.

\noindent The retraction is summarised by
\[
\begin{array}{c|c|c}
\mathrm{Item} & \mathrm{Retracted} & \mathrm{Corrected}\\ \hline
\mathrm{remainder} & 2.7\times10^{-5} & 1.65\times10^{-3}\\
\mathrm{protection} & \times130 & \times2\\
\mathrm{identity} & M'=-J(0) & M'=-J(0)/2
\end{array}
\]
The corrected common-mode residual remains below the certified protection margin after the factor-2 convention repair.

\section*{3. Structure of the argument}

The near-gap common-mode is repaired and the convention re-derived to exactness; the corrected $M'=-J(0)/2$ identity (a factor-2 convention fix) propagates to the protection ratio. The earlier over-claim is explicitly retracted and the corrected margins recorded -- a self-caught audit.

\section*{4. Numerical reproduction}

The common-mode repair script reproduces the corrected remainder $1.65\times10^{-3}$ and the $\times2$ protection; run it from the bundle.

\section*{5. Devil's-advocate / self-adversarial review (CLAUDE.md 6.3)}

\begin{itemize}
\item[($\alpha$)] \emph{Attack the scope.} The result is confined to the scope stated in
Sec.~1; a referee may push that boundary, which is fair --- the package makes no claim
outside it, and the precise scope is in the footer.
\item[($\beta$)] \emph{Attack the tier / over-claim.} No prose here is stronger than the
registered grade (footer no-overclaim line); the status is REVIEW DRAFT, NOT
operator-confirmed, and the claim-ledger tier is the arbiter.
\item[($\gamma$)] \emph{Attack reproduction / self-containment.} Each reproduction script
carries self-test asserts and ships in the folder's bundle with its transitive
dependencies (imports and runtime file reads, resolver v1.3.0); a referee runs them and
diffs against \texttt{expected/}.
\end{itemize}

Quantitative sanity check (mandatory): the falsifier (footer) is concrete and
pre-registered, so the claim is refutable by a single explicit construction; and the
numerical values it cites are reproduced by the folder's scripts (all asserts pass).

\section*{6. Result footer}

{\footnotesize
\begin{verbatim}
Result ID:               B1-RH-ENUM / near-gap referee package (DRAFT)
Precise statement:       R-U10-1's O(I^2) claims are SUPERSEDED by
                         useries-triage (remainder 1.65e-3 not 2.7e-5;
                         protection x2 not x130; identity M'=-J(0)/2
                         not -J(0)); with the corrected convention the
                         near-gap residual closes with explicit margins
                         via the common-mode repair.
Scope:                   near-gap convention exactness + common-mode
                         repair; includes a self-caught retraction.
Evidence grade:          convention-exact closure + retraction record
                         (EXECUTED).
Expected output:         remainder 1.65e-3, protection x2,
                         M'=-J(0)/2; script PASS.
Reproduction command:    python
                         codes/vacuum/neargap_common_mode_repair.py
Falsifier:               a near-gap configuration where the corrected
                         margins fail; or a re-derivation contradicting
                         M'=-J(0)/2.
Tier before / after:     DRAFT referee package; convention closure +
                         retraction. DRAFT ACCEPTED (operator
                         2026-06-11); published-bundle HELD.
No-overclaim statement:  the corrected (factor-2) convention is the
                         claim; the prior O(I^2) numbers are withdrawn.
Next required action:    inspect 1-script reproduction bundle ->
                         operator PUBLISHED confirm.
\end{verbatim}
}
