# B1-RH-ENUM literature applicability

**Claim:** `B1-RH-ENUM`
**Reviewed:** 2026-08-14
**Overall disposition:** `APPLIES`
**Load-bearing use:** Yes, only for the registered `C_full` Reading-H comparison against `G_*`.
**Authority rule:** `status.json` is the live claim authority. The older prose in `claim.md` is retained as compatibility history and is not used to reconstruct the current tier or scope.

## 1 Exact target and intended role

The imported target is only the live internal comparison

`F_total[Q] > F_total[G_*]`

for every admitted `Q` in the registered `C_full` class and parameter region `R`, given the `A1-KERNEL-CONV` definition. The intended role is to stop re-proving the already registered candidate-versus-isotropic-reference ordering inside that owner.

This target is not `F_total[G_*]-F_total[physical empty] < 0`. It is not a Reading-H-to-P1 functional map, a proof that a BCC realization is the vacuum, or a premise for `C6-SPACETIME-SIGNATURE`.

## 2 Stable source locator

- Live claim state: `claims/B1-RH-ENUM/status.json`, SHA-256 `ac08c4be7b0afa16744bc790054b4703124f58a903aa9ac455c0e94146149a8a`.
- Current C-full referee source: `claims/B1-RH-ENUM/Reading-H/notes/reading-h-cfull-referee-package-260610-v1.1.tex.txt`, especially lines 24-58, 159-166, and 207-239, SHA-256 `3a4b2d853992d2b4495da7a4ad1619fda21170f72247f3d549a0bb051680a98f`.
- Immutable reproduction manifest: `claims/B1-RH-ENUM/bundle/Reading-H-cFull-T7-260611/MANIFEST.json`, SHA-256 `d93bbfaa83cbcd35d3fabc531b84b50b6ba69c28535d01baa8cea69ed273ff7f`.
- Pattern-universal conditional antecedent: `archive/legacy/notes/Math437/TECT-Math437-Step5-Pattern-Universal-Restoration-Isotropic-Layer-260604-v1.2.tex.txt`, Proposition A at lines 162-234, SHA-256 `aca70d836787eaae5fc51b015c4e283dd02ac478c2d917671e8e493d47f70edd`.
- Native-owner scope firewall: `strategy/pre-a-t055-reading-h-native-owner-bcc-route-termination-manifest.json`, SHA-256 `3ff66a08efa8838f01da13e5aeacae5bcc5a9de6d696b28ddfbaae9d3a724c21`.

These are current or immutable in-repository sources. They are not external-literature substitutes and they do not identify `G_*` with physical empty space.

The bounded selected-index search was run on 2026-08-14 with
`python verification/scripts/legacy_search.py query --text "Reading-H Gaussian Hartree G_* C_full" --claim B1-RH-ENUM --limit 50 --json`.
It returned 50 rows spanning 22 unique source IDs. The limit was saturated, so this is a reproducible audit window rather than a total-corpus count; the stable current sources above, not the hit count, own the imported conclusion.

## 3 Assumption-to-model crosswalk

| Source hypothesis | TECT object or authority | Disposition | Load-bearing for target | Reason |
|---|---|---|---|---|
| The A1 kernel convention defines the compared functional | `A1-KERNEL-CONV` | `SATISFIED` | yes | It is the named definitional input of the live B1 card. |
| The competitor belongs to the real-antipodal, coherence- and packing-controlled `C_full` class | Candidate admission contract in the C-full package | `CONDITIONAL` | yes | The theorem applies after, not before, membership is proved for the candidate. |
| `(I,mu2)` lies in the registered region `R` | Live B1 scope | `SATISFIED` | yes | The production point `(1/500,1/200)` is inside the registered scope. |
| `G_*` is the isotropic Reading-H reference used by the same comparison | C-full referee package | `SATISFIED` | yes | Both sides of the imported inequality use that owner and normalization. |
| The trial-layer argument extends to the live C-full theorem | Current B1 package and status | `SATISFIED` | yes | The live authority, not the conditional Math437 note alone, owns the registered comparison. |
| `G_*` is a preregistered physical-empty state | No current authority | `FAILED` | no | `G_*` is the fluctuation-disordered Reading-H reference with nonzero covariance, not a third matched empty comparator. |
| Reading-H has a hash-identical P1 or full-H functional map | R-169 interface audit | `FAILED` | no | The field, ensemble, normalization, and energy intertwiner are absent. |
| The comparison has a controlled physical thermodynamic/continuum limit | No current authority | `UNASSESSED` | no | The imported theorem remains inside its registered owner and scope. |

## 4 Exact imported conclusion and scope

The imported conclusion is exactly the strict relative ordering `F_total[Q] > F_total[G_*]` for all admitted `Q` in `C_full` and the registered parameter region `R`, given `A1-KERNEL-CONV`. The object is a finite real-antipodal point set with amplitudes on the three-dimensional Brazovskii momentum shell `|k|=q0`, compared in the diagonal Gaussian-Hartree trial layer with the rotation-invariant shell dressing `G_*`. The state class is `C_full`; the scalar parameters are `(I,mu2)` in `R`; the algebra is the declared finite shell-amplitude and Gaussian-covariance variational bookkeeping. The imported statement quantifies no physical spatial volume, finite-volume sequence, UV cutoff, lattice-spacing regulator, thermodynamic limit, continuum limit, or zero-temperature limit. Its normalization and comparison reference are exactly the common `F_total` owner of the live B1 package.

No physical finite-volume-to-infinite-volume theorem, continuum removal, zero-temperature limit, P1 identity, absolute vacuum normalization, or spacetime conclusion is imported.

## 5 Reproduction and independent-check disposition

The immutable C-full bundle supplies five executable checks with the registered aggregate `27/27` PASS contract; the broader live B1 card separately records the eight-script `167`-assert migration revalidation. The package also contains the operator adversarial chain and the Math440/Math441 second-look repairs of Proposition A. These checks support reuse at the live registered strength. A script PASS is not treated as an independent analytic proof, and an external publication referee remains a validation item rather than a premise for this applicability classification.

## 6 Adversarial checks

- **Convention/sign - VALID with scope pin.** The imported sign is candidate minus `G_*`. Reversing the order or replacing `G_*` by physical empty changes the theorem.
- **Reference identity - UPHELD objection.** The `A=0` Reading-H reference is not a third physical-empty branch; no absolute-vacuum sign follows.
- **Domain/regularity - VALID with condition.** A future candidate must prove membership in `C_full` and the region `R`; geometry or a covariance label alone is insufficient.
- **Limit/order-of-limits - UPHELD objection outside scope.** No thermodynamic, continuum, regulator-removal, or zero-temperature transfer is imported.
- **Proof versus execution - VALID with mitigation.** Executed constants and source hashes accompany a written argument; neither alone silently widens the live card.

## 7 Residual proposition or stop decision

Stop re-proving the B1 `Q > G_*` comparison. The first genuinely new proposition is a same-parent, same-ensemble, matched-normalization theorem that defines a preregistered physical-empty branch `E`, fixes all state-dependent finite parts, and decides `sign(F_total[G_*]-F_total[E])` with controlled limits. The Reading-H-to-P1 interface is a separate residual.

## 8 No-overclaim boundary

This record changes no B1 tier or lifecycle. It proves no physical-empty sign, P1 bridge, BCC vacuum, transverse stability, C6 result, Round-1 admission, physical Sector A result, or Pre-A closure.
