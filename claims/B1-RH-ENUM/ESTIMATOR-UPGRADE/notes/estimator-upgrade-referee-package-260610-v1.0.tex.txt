% SUPERSEDED by estimator-upgrade-referee-package-260610-v1.1.tex.txt (operator review 2026-06-10: ACCEPT T4 draft, HOLD published confirm; explicit 22-script manifest + SMA/multishell scope sentence)
% =============================================================================
% Title: Referee package (DRAFT) -- the enumerated Reading-H selection margin upgraded to a controlled-error, node-free no-condensate statement
% Claim: B1-RH-ENUM (referee package; pending operator confirmation)
% Version: v1.0 -- first issued 2026-06-10; this version issued 2026-06-10
% Status: REFEREE PACKAGE DRAFT -- NOT operator-confirmed. Folder result grade T4 (STRONG EVIDENCE) within the enumerated-ensemble scope. Self-contained
%   referee-facing assembly for external review; pending the operator-confirmation gate
%   (governance/reproduction-bundle-policy.md sec.11) before a PUBLISHED bundle is built.
%   Reproduction: 22 script(s) in this folder's bundle. Grounded by the folder's notes.
%
% Revision history (cumulative):
%   v1.0 (2026-06-10) first DRAFT issue for operator review. Synthesises the folder's
%        headline result into a self-contained referee document. Awaiting operator confirm.
% =============================================================================

\sloppy
\section*{1. Purpose and scope}

This self-contained referee document concerns the ESTIMATOR-UPGRADE sub-proof of B1-RH-ENUM: the upgrade of the estimator-grade Reading-H selection $\Delta F>0$ to a CONTROLLED-ERROR, node-free (continuum) no-condensate statement, across all numerical quadrature knobs and for a multi-shell ensemble's exact (off-diagonal-inclusive) free energy. \textbf{Scope:} the enumerated competitor ensemble at the operating point; single-shell SMA with the $(M,\delta I,$ amplitude-grid) knobs. \textbf{Not claimed:} this is not the full $\mathcal C_{\rm full}$ selection (that is the Reading-H package); the folder result is T4 STRONG EVIDENCE, not a theorem.

\section*{2. The result}

For the enumerated Reading-H ensemble, the estimator-grade $\Delta F>0$ is promoted to a node-free no-condensate certificate by combining (a) grid-independent curvature certification at two quadrature resolutions, (b) a curvature-chord interval lower bound $\Delta F\ge\min(v_i,v_{i+1})-\tfrac18 M_i \delta^2$, and (c) a near-reference positive-definite Hessian covering the thin region where the off-diagonal correction is $O(\text{amplitude}^4)$. The exact (slogdet) engine is re-used by neuter-import of a frozen legacy script, validated against its recorded outputs to $5\times10^{-8}$.

\section*{3. Structure of the argument}

(i) The estimator $\Delta F$ is reproduced at two quadrature resolutions and the curvature is certified grid-independent. (ii) The interval lower bound makes the no-condensate conclusion robust to discretisation. (iii) The exact-Wick off-diagonal bracket is shown $O(\text{amplitude}^4)$ near the reference, so it never shifts the $(0,0)$ Hessian, which stays equal to the diagonal one. (iv) A physical secondary-shell ratio is shown to lie interior to a region already certified condensate-free. The knob-completeness (single-shell SMA, amplitude grid, trial mass) is exercised script-by-script.

\section*{4. Numerical reproduction}

The folder's reproduction scripts (in the bundle) each carry self-test asserts and exit 0 iff all pass; the union covers the controlled-error certification, the knob sweeps, and the engine-reuse validation. Run each \texttt{python codes/.../<script>.py}; compare against \texttt{expected/}.

\section*{5. Devil's-advocate / self-adversarial review (CLAUDE.md 6.3)}

\begin{itemize}
\item[($\alpha$)] \emph{Attack the scope.} The result is confined to the scope stated in
Sec.~1; a referee may push that boundary, which is fair --- the package makes no claim
outside it, and the precise scope is in the footer.
\item[($\beta$)] \emph{Attack the tier / over-claim.} No prose here is stronger than the
registered grade (footer no-overclaim line); the status is REVIEW DRAFT, NOT
operator-confirmed, and the claim-ledger tier is the arbiter.
\item[($\gamma$)] \emph{Attack reproduction / self-containment.} Each reproduction script
carries self-test asserts and ships in the folder's bundle with its transitive
dependencies (imports and runtime file reads, resolver v1.3.0); a referee runs them and
diffs against \texttt{expected/}.
\end{itemize}

Quantitative sanity check (mandatory): the falsifier (footer) is concrete and
pre-registered, so the claim is refutable by a single explicit construction; and the
numerical values it cites are reproduced by the folder's scripts (all asserts pass).

\section*{6. Result footer}

{\footnotesize
\begin{verbatim}
Result ID:               B1-RH-ENUM / ESTIMATOR-UPGRADE referee package
                         (DRAFT)
Precise statement:       The enumerated Reading-H estimator-grade dF>0
                         is upgraded to a controlled-error, node-free
                         (continuum) no-condensate statement across all
                         quadrature knobs and for the multi-shell exact
                         free energy, via two-resolution curvature
                         certification + curvature-chord interval bound
                         + near-reference PD Hessian + frozen-engine
                         neuter-import (validated 5e-8).
Scope:                   enumerated competitor ensemble, operating
                         point, single-shell SMA knobs. T4 STRONG
                         EVIDENCE.
Evidence grade:          controlled-error continuum no-condensate (T4);
                         EXECUTED (folder scripts PASS).
Reproduction command:    python codes/vacuum/estimator_upgrade_*.py ;
                         res_endpoint_admissible_pin.py ; ...
Falsifier:               an enumerated competitor with dF<=0 surviving
                         the controlled-error bound; or a knob setting
                         that flips the curvature certificate.
Tier before / after:     DRAFT referee package; folder result T4. NOT
                         operator-confirmed.
No-overclaim statement:  controlled-error STRONG EVIDENCE within the
                         enumerated-ensemble scope; not the full C_full
                         selection, not a theorem.
Next required action:    operator review + confirm -> then build the
                         PUBLISHED bundle.
\end{verbatim}
}
