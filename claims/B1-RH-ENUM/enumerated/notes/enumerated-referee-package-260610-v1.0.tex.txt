% =============================================================================
% Title: Referee package (DRAFT) -- migration and full re-validation of the enumerated-reading evidence chain (batch 2, 167/167)
% Claim: B1-RH-ENUM (referee package; pending operator confirmation)
% Version: v1.0 -- first issued 2026-06-10; this version issued 2026-06-10
% Status: AUXILIARY (not a main-line referee artefact; cited by the main line, DRAFT
%   bundle only, NOT promoted to PUBLISHED -- main-proof-line.md, operator-confirmed 2026-06-11).
%   REFEREE PACKAGE DRAFT -- NOT operator-confirmed. Folder result: migration re-validation record (provenance, not a new theorem). Self-contained
%   referee-facing assembly for external review; pending the operator-confirmation gate
%   (governance/reproduction-bundle-policy.md sec.11) before a PUBLISHED bundle is built.
%   Reproduction: 1 script(s) in this folder's bundle. Grounded by the folder's notes.
%
% Revision history (cumulative):
%   v1.0 (2026-06-10) first DRAFT issue for operator review. Synthesises the folder's
%        headline result into a self-contained referee document. Awaiting operator confirm.
% =============================================================================

\sloppy
\section*{1. Purpose and scope}

This referee document records the MIGRATION and full re-validation of the enumerated-reading evidence chain (migration batch 2) into the verification-first repository. \textbf{Scope:} provenance and reproducibility of the migrated enumerated-reading results. \textbf{Not claimed:} no new theorem; this certifies that the migrated chain re-executes identically.

\section*{2. The result}

The migration batch-2 evidence chain is migrated verbatim and re-validated by full re-execution: $167/167$ checks pass. The enumerated-reading results therefore carry an unbroken, re-runnable provenance trace under the current governance.

\section*{3. Structure of the argument}

The legacy enumerated-reading scripts are migrated with their constants and re-executed end-to-end; the $u$-series mainline checks (\texttt{t6\_mainline\_useries\_checks}) reproduce all $167$ asserts. The chain's claim-to-evidence links are re-established in the ledger.

\section*{4. Numerical reproduction}

The mainline $u$-series check script reproduces the $167/167$ result; run it from the bundle and compare against \texttt{expected/}.

\section*{5. Devil's-advocate / self-adversarial review (CLAUDE.md 6.3)}

\begin{itemize}
\item[($\alpha$)] \emph{Attack the scope.} The result is confined to the scope stated in
Sec.~1; a referee may push that boundary, which is fair --- the package makes no claim
outside it, and the precise scope is in the footer.
\item[($\beta$)] \emph{Attack the tier / over-claim.} No prose here is stronger than the
registered grade (footer no-overclaim line); the status is REVIEW DRAFT, NOT
operator-confirmed, and the claim-ledger tier is the arbiter.
\item[($\gamma$)] \emph{Attack reproduction / self-containment.} Each reproduction script
carries self-test asserts and ships in the folder's bundle with its transitive
dependencies (imports and runtime file reads, resolver v1.3.0); a referee runs them and
diffs against \texttt{expected/}.
\end{itemize}

Quantitative sanity check (mandatory): the falsifier (footer) is concrete and
pre-registered, so the claim is refutable by a single explicit construction; and the
numerical values it cites are reproduced by the folder's scripts (all asserts pass).

\section*{6. Result footer}

{\footnotesize
\begin{verbatim}
Result ID:               B1-RH-ENUM / enumerated referee package (DRAFT)
Precise statement:       The enumerated-reading evidence chain
                         (migration batch 2) is migrated verbatim and
                         re-validated by full re-execution: 167/167
                         checks pass; the results carry a re-runnable
                         provenance trace.
Scope:                   provenance + reproducibility of the migrated
                         enumerated-reading chain. Re-validation record.
Evidence grade:          migration re-validation (167/167 EXECUTED).
Reproduction command:    python
                         codes/vacuum/t6_mainline_useries_checks.py
Falsifier:               any of the 167 checks failing on re-execution
                         from a clean checkout.
Tier before / after:     DRAFT referee package; provenance record. NOT
                         operator-confirmed.
No-overclaim statement:  a migration/re-validation record, not a new
                         mathematical result.
Next required action:    operator review + confirm -> PUBLISHED bundle.
\end{verbatim}
}
