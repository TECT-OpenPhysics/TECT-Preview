% =============================================================================
% Title: Referee package (DRAFT) -- H-A0 to H-ANCHOR: A-zero uniqueness as a sign-decomposition theorem
% Claim: B2-PROPA-HLAYER (referee package; pending operator confirmation)
% Version: v1.1 -- first issued 2026-06-10; this version issued 2026-06-11
% Status: AUXILIARY (not a main-line referee artefact; cited by the main line, DRAFT
%   bundle only, NOT promoted to PUBLISHED -- main-proof-line.md, operator-confirmed 2026-06-11).
%   REFEREE PACKAGE DRAFT -- AUXILIARY DRAFT ACCEPTED (operator 2026-06-11); NOT promoted to a main-line published referee package (cited as support only). Folder result grade T6/T7 (sign-decomposition theorem). Self-contained
%   referee-facing assembly for external review; pending the operator-confirmation gate
%   (governance/reproduction-bundle-policy.md sec.11) before a PUBLISHED bundle is built.
%   Reproduction: 1 script(s) in this folder's bundle. Grounded by the folder's notes.
%
% Revision history (cumulative):
%   v1.0 (2026-06-10) first DRAFT issue for operator review. Synthesises the folder's
%        headline result into a self-contained referee document. Awaiting operator confirm.
%   v1.1 (2026-06-11) operator review: AUXILIARY DRAFT ACCEPTED (sign-decomposition
%        theorem, not promoted). Patch: explicit zero-at-gap anchor equality g(m_gap)=0;
%        expected output.
% =============================================================================

\sloppy
\section*{1. Purpose and scope}

This referee document concerns the replacement of the H-A0 hypothesis by H-ANCHOR via a sign-decomposition theorem for $A=0$ uniqueness and zero-at-gap. \textbf{Scope:} the free-energy derivative $F_0'(m)$ on the window. \textbf{Not claimed:} only the sign-decomposition and its discharge of H-A0's $U,Z$ conditions.

\section*{2. The result}

$A$-zero uniqueness and zero-at-gap (H-A0's $U,Z$) are derived as a sign-decomposition theorem: $F_0'(m)=\tfrac12 M'(m)\,g(m)$ with $M'<0$ (L1), $g$ strictly decreasing on the window (L2), and $g<0$ beyond the gap (L3); hence $F_0'$ has a unique sign change, discharging H-A0. Explicitly, $g(m_{\rm gap})=0$, $g'(m)<0$ on the certified window, and $g(m)<0$ for $m>m_{\rm gap}$; the unique sign change of $F_0'(m)$ occurs at the anchored gap point because $g(m_{\rm gap})=0$ and $M'(m)<0$.

\section*{3. Structure of the argument}

Factor the derivative as $F_0'=\tfrac12 M' g$. L1 gives $M'<0$ (dominated convergence, the G-A0-DUI lemma). L2 shows $g$ strictly decreasing (closed form). L3 shows $g<0$ beyond the gap. The product's sign is therefore determined, yielding a unique stationary point at $A=0$ and the zero-at-gap property -- exactly H-A0's $U,Z$, now theorems rather than hypotheses.

\section*{4. Numerical reproduction}

The sign-decomposition script reproduces the $M'<0$, $g$-decreasing and $g<0$ facts and the unique sign change with asserts; run it from the bundle. The expected output is $M'(m)<0$, $g'(m)<0$, $g(m)<0$ beyond the gap, and no second stationary point.

\section*{5. Devil's-advocate / self-adversarial review (CLAUDE.md 6.3)}

\begin{itemize}
\item[($\alpha$)] \emph{Attack the scope.} The result is confined to the scope stated in
Sec.~1; a referee may push that boundary, which is fair --- the package makes no claim
outside it, and the precise scope is in the footer.
\item[($\beta$)] \emph{Attack the tier / over-claim.} No prose here is stronger than the
registered grade (footer no-overclaim line); the status is REVIEW DRAFT, NOT
operator-confirmed, and the claim-ledger tier is the arbiter.
\item[($\gamma$)] \emph{Attack reproduction / self-containment.} Each reproduction script
carries self-test asserts and ships in the folder's bundle with its transitive
dependencies (imports and runtime file reads, resolver v1.3.0); a referee runs them and
diffs against \texttt{expected/}.
\end{itemize}

Quantitative sanity check (mandatory): the falsifier (footer) is concrete and
pre-registered, so the claim is refutable by a single explicit construction; and the
numerical values it cites are reproduced by the folder's scripts (all asserts pass).

\section*{6. Result footer}

{\footnotesize
\begin{verbatim}
Result ID:               B2-PROPA-HLAYER / H-A0-removal referee package
                         (DRAFT)
Precise statement:       A-zero uniqueness + zero-at-gap (H-A0's U,Z)
                         derived as a sign-decomposition theorem:
                         F_0'(m)=(1/2)M'(m)g(m) with M'<0 (L1), g
                         strictly decreasing on the window (L2), g<0
                         beyond the gap (L3); unique sign change
                         discharges H-A0.
Scope:                   the free-energy derivative F_0'(m) on the
                         window; H-A0 discharge.
Evidence grade:          sign-decomposition theorem (T6/T7); EXECUTED.
Expected output:         M'(m)<0; g'(m)<0; g(m_gap)=0; g(m)<0 beyond
                         the gap; no second stationary point.
Reproduction command:    python codes/vacuum/ha0_sign_decomposition.py
Falsifier:               a window point violating M'<0 or
                         g-monotonicity; or a second stationary point.
Tier before / after:     DRAFT referee package; H-A0 discharged to
                         H-ANCHOR. AUXILIARY DRAFT ACCEPTED
                         (2026-06-11); not promoted to published.
No-overclaim statement:  H-A0's U,Z become theorems on the window; not
                         the full Prop-A comparison.
Next required action:    remain an auxiliary cited lemma (DRAFT bundle
                         only); not promoted to a published package.
\end{verbatim}
}
