% =============================================================================
% Title: Referee package -- Proposition A: the isotropic Gaussian-Hartree layer is the strict diagonal minimum, and the off-diagonal reduces to the additive-energy floor (class-wide H-LAYER closure)
% Claim: B2-PROPA-HLAYER (referee package; pending operator confirmation)
% Version: v1.3 -- first issued 2026-06-10; this version issued 2026-06-11
% Status: REFEREE PACKAGE -- MAIN-LINE SUPPORTING PACKAGE, PUBLISHED-BUNDLE CONFIRMED (operator 2026-06-11); published as Prop-A-T6-260611 (T6, scope A_adm T'<=13); does NOT independently enact the full C_full Reading-H theorem. Folder result grade T6/T7 on the primary shell-supported class A_adm. Self-contained
%   referee-facing assembly for external review; pending the operator-confirmation gate
%   (governance/reproduction-bundle-policy.md sec.11) before a PUBLISHED bundle is built.
%   Reproduction: 9 script(s) in this folder's bundle. Grounded by the folder's notes.
%
% Revision history (cumulative):
%   v1.0 (2026-06-10) first DRAFT issue for operator review. Synthesises the folder's
%        headline result into a self-contained referee document. Awaiting operator confirm.
%   v1.1 (2026-06-11) operator review: MAIN-LINE SUPPORTING DRAFT ACCEPTED, published-
%        bundle HELD. Patches: explicit 9-script manifest (no wildcard); (D) global/convex
%        globality caveat; (S) one-line joint>1 <=> T'<60.4 derivation; expected output.
%   v1.2 (2026-06-11) operator PUBLISHED-BUNDLE CONFIRMED after reconstructed clean-run
%        (9/9 scripts, 44/44 asserts PASS); published as Prop-A-T6-260611. Added the
%        publication qualifier and the T-019 supersession note for hdiag_gershgorin_rowsum.
%   v1.3 (2026-06-11) operator metadata patch: cleared residual DRAFT wording (Title,
%        Sec.5 over-claim line, footer Result ID) to PUBLISHED / Prop-A-T6-260611.
% =============================================================================

\sloppy
\section*{1. Purpose and scope}

This referee document concerns Proposition A (the Prop-A folder of B2-PROPA-HLAYER): that the isotropic Gaussian-Hartree layer $G_*$ is the strict comparison minimum, with the H-LAYER residuals closed class-wide on the primary shell-supported competitor class $\mathcal A_{\rm adm}$ ($T'\le13$). \textbf{Scope:} $\mathcal A_{\rm adm}$ (registered crystalline shell / shell-union, $T'\le13$) at the operating point. \textbf{Not claimed:} the full $\mathcal C_{\rm full}$ comfortable extension is the Reading-H package; here the margin on $\mathcal A_{\rm adm}$ is the comfortable $T'\le13$ one.

\section*{2. The result}

The T-016..T-024 programme closes every ANALYTIC H-LAYER residual class-wide on $\mathcal A_{\rm adm}$: (D) the diagonal Hessian at $G_*$ is block-diagonal in angular momentum with $l\ge1$ curvature $\tfrac12 G_*^{-2}\ge\tfrac12 r_R^2>0$ and $l=0$ adding $\tfrac32 u_{\rm eff}>0$; (O) the off-diagonal reduces to the additive-energy floor with $R_{\rm lead}(13)=0.650<1$; (S) the third-cumulant joint $=\times1.097>1$ with the analytic threshold $T'<60.4$.

\section*{3. Structure of the argument}

(D) An arbitrary perturbation $\delta G$ is decomposed in spherical harmonics; the rotation-invariant interaction curves only $l=0$, so the Hessian is block-diagonal and strictly positive in every sector. The strict diagonal-\emph{minimum} conclusion invokes the global/convex component of T-016; the Hessian computation alone gives local strict stability. (O) The condensate block is diagonal in the channel index (a sum of independent channel terms), so its operator norm is the worst single-channel ratio $R_{\rm lead}=23.2(1+K_{\rm floor})I$, bounded by the floor pin $K_{\rm floor}\le T'\le13$. (S) MARGIN cancels in the certified joint, leaving a function of $\rho_{\rm lat}$ with $\mathrm{joint}>1\iff T'<60.4$; since $T'\le13<60.4$ on $\mathcal A_{\rm adm}$, the joint evaluates to $\mathrm{joint}=\times1.097>1$.

\section*{4. Numerical reproduction}

The nine reproduction scripts (each with self-test asserts, each exiting 0) reproduce the class-wide second-cumulant stability, the Gershgorin row-sums, the diagonal convexity, the off-diagonal additive-energy floor and the constant certificate. The full manifest (no wildcard) is:

\begin{verbatim}
9 reproduction scripts (codes/vacuum/; each asserts, exits 0):
  (D) diagonal isotropy / convexity:
        hdiag_convexity_probe.py
        res5_016_isotropy_infimum_core.py
  (O) off-diagonal additive-energy floor + operator norm:
        hdiag_offdiag_additive_energy.py
        hdiag_offdiag_constant_certificate.py
        res1_hdiag_offdiag_floor.py
        res5_019_exchange_scalar_identification.py
        hdiag_gershgorin_rowsum.py
  (S) third-cumulant selection (class-wide):
        res5_020_classwide_secondcumulant_stability.py
  route audit:
        res5_029_t7_route_audit.py
\end{verbatim}

Run them from the bundle and diff against \texttt{expected/}. \emph{Supersession note:} \texttt{hdiag\_gershgorin\_rowsum.py} is retained as a row-sum / additive-energy support script; its older ``exchange sign not fixed by $u_{\rm eff}>0$'' framing is superseded by T-019 (\texttt{res5\_019}), which reframes the TECT local interaction as having no $A$-independent attractive Fock exchange and a stabilising mean-field off-diagonal Hessian $f''(M)=\tfrac32 u_{\rm eff}>0$. The live route uses $R_{\rm lead}<1$ and the T-019 reframing; the audit \texttt{res5\_029} records this supersession.

\section*{5. Devil's-advocate / self-adversarial review (CLAUDE.md 6.3)}

\begin{itemize}
\item[($\alpha$)] \emph{Attack the scope.} The result is confined to the scope stated in
Sec.~1; a referee may push that boundary, which is fair --- the package makes no claim
outside it, and the precise scope is in the footer.
\item[($\beta$)] \emph{Attack the tier / over-claim.} No prose here is stronger than the
registered grade (footer no-overclaim line); the status is PUBLISHED-BUNDLE CONFIRMED within the stated $\mathcal A_{\rm adm}$ scope; the claim-ledger tier remains T6 and does not enact the full $C_{\rm full}$ Reading-H theorem.
\item[($\gamma$)] \emph{Attack reproduction / self-containment.} Each reproduction script
carries self-test asserts and ships in the folder's bundle with its transitive
dependencies (imports and runtime file reads, resolver v1.3.0); a referee runs them and
diffs against \texttt{expected/}.
\end{itemize}

Quantitative sanity check (mandatory): the falsifier (footer) is concrete and
pre-registered, so the claim is refutable by a single explicit construction; and the
numerical values it cites are reproduced by the folder's scripts (all asserts pass).

\section*{6. Result footer}

{\footnotesize
\begin{verbatim}
Result ID:               B2-PROPA-HLAYER / Prop-A-T6-260611
Precise statement:       On the primary shell-supported class A_adm
                         (T'<=13), the T-016..T-024 programme closes
                         every analytic H-LAYER residual: (D)
                         block-diagonal isotropy Hessian (entropy floor
                         (1/2)r_R^2>0, breathing (3/2)u_eff>0); (O)
                         off-diagonal = additive-energy floor with
                         R_lead(13)=0.650<1; (S) third-cumulant
                         joint=x1.097>1 (analytic threshold T'<60.4).
Scope:                   A_adm (registered crystalline
                         shell/shell-union, T'<=13), operating point.
                         Comfortable margin.
Evidence grade:          class-wide analytic closure on A_adm (T6/T7);
                         EXECUTED (9 scripts PASS).
Expected output:         R_lead(13)=0.650<1; joint=x1.097>1;
                         T'_threshold=60.4; all 9 scripts PASS.
Reproduction command:    run the 9 scripts listed in Sec.4
                         (codes/vacuum/; each exits 0); full manifest
                         in Sec.4, no wildcard.
Falsifier:               an A_adm competitor with R_lead>=1 or
                         joint<=1; or a perturbation breaking the
                         block-diagonal positivity.
Tier before / after:     A_adm closure, T6 (main-line supporting).
                         PUBLISHED-BUNDLE CONFIRMED (operator
                         2026-06-11): Prop-A-T6-260611.
No-overclaim statement:  class-wide closure on A_adm (T'<=13,
                         comfortable); the full C_full extension is the
                         Reading-H package.
Next required action:    none for this package (PUBLISHED as
                         Prop-A-T6-260611); the full C_full closure is
                         the separate Reading-H package.
Publication:             confirmed after reconstructed clean-run from
                         the uploaded bundle: 9/9 scripts, 44/44
                         asserts PASS. Scope A_adm, T'<=13; does NOT
                         independently enact full C_full Reading-H.
\end{verbatim}
}
