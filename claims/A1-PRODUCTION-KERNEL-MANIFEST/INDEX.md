# A1-PRODUCTION-KERNEL-MANIFEST — proof-unit index

**Claim**: Production kernel manifest: parameter-consistency gates (current config is a mock)  
**Tier**: T1 (ACTIVE)  ·  **Hypotheses**: —  ·  **Open gates**: —

> GENERATED by `build_index.py` — do not hand-edit. Each row is one proof unit (current note version). `Tier*` = inherits the claim tier (note declares no tier change). Sub-proofs are the **proposed** grouping (no files moved yet).

**Selection statement.** A config stores (r_zero, mu2_shell, q0, Z, Y, eta_shell) + a 7-key scalar_slice (laplacian_mode, bcc_mix_epsilon, a_bcc, family_masses, k_lock, z0, eta_shell). The N-001 solver symbol is bloch_matrix_linear(|k|,{r,Z,Y,q0,**scalar_slice}); (r,Z)=kinetic_coefficients(mu2_shell,Y,q0). Gates delta_Z,delta_m,delta_r (separ…

**Falsifier.** A config passing all gates with m_sh^2>0 closes the OPEN status for that config (admits it as production).

## (all notes)/  ·  2 proof units  ·  tier span —

| Proof unit | Cur | Tier | What it proves (footer: precise statement) | Evidence | Next action |
|---|---|---|---|---|---|
| `a1-n001-analytic-branch-certification` | v1.0 | — |  |  |  |
| `a1-production-kernel-manifest` | v1.5 | — | Verifier asserts config schema complete, reads 6 physical fields + the 7-key scalar_slice from canonical_n001_kernel .json, imports the ORIGINAL solv… | ANALYTIC + EXECUTED (14/14) -- pending operat… | Operator independently runs the bundle (14/14) and certifies. |

<!-- generated 2026-06-23T12:25:52Z by build_index.py v1.0.1 -->
