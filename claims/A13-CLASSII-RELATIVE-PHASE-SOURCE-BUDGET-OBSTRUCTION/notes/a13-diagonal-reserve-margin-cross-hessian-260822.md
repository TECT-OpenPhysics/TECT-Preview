# R-183 note: diagonal reserve margin for the pulled cross-Hessian

R-183 records the exact finite reserve condition forced by the R-182 negative
cross block. For cross magnitude `a`, reserves `d1,d2` yield
`p=d1-a`, `q=d2-a`; the beta=1/2 pullback is nonnegative whenever
`p>0` and `q>=a^2/p`, and the witness `(-a/p,1)` proves this condition is
necessary for the original form. Isotropic reserves require `d>=2a`.

At the registered active scale `a=8`, the exact isotropic threshold is 16 and
the subthreshold value `d=15` has the relative-phase fixture `-2`. This is a
finite conditional margin criterion. The actual heat, forest, complement,
returned-low, source and sextic owners have not yet been identified with the
diagonal reserves, so no production sign or gate closure follows.

The accompanying Lean, SymPy, stdlib Fraction and integrated lanes are
claim-nonbearing and reject scope, provenance, hash, independence and Lean
escape-token mutations.
