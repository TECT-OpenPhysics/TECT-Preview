# R-174 two-root production-cylinder cross-synthesis

R-174 is a T0, claim-nonbearing exact interface checkpoint. It joins the
registered A1 `k,2k` torus chart to the one-root R-150 synthesis and the
two-root R-151 boundary. The exact cross blocks are retained rather than
replaced by a diagonal covariance sum.

For phase rows `X_j=(a_j c_j,a_j s_j)` and derivative rows
`V_j=w_j(-a_j s_j,a_j c_j)`, direct expansion gives

`X_1 V_2^*=w_2 a_1a_2(s_1c_2-c_1s_2)`,
`X_2 V_1^*=-w_1 a_1a_2(s_1c_2-c_1s_2)`,
and the summed cross block
`(w_2-w_1)a_1a_2(s_1c_2-c_1s_2)`. The field and current cross blocks
carry the common phase factor `c_1c_2+s_1s_2`. A scalar fixture gives `-1`,
so the cross block is not an artifact of a symbolic zero.

The Lean kernel checks only these rational polynomial identities. The primary
lane also reconstructs the exact A1 family-lock mass and verifies the positive
`k` and `2k` scalar symbols; the independent lane is Fraction-only. This is a
joint-cylinder data requirement, not a sign theorem. The complete owner still
needs square-root bases, root/heat incidence, complement and low placement,
forest and returned mean, source and sextic terms, and the legal one-use
allocation.

Adversarial review rejects: dropping the ordered cross blocks, treating
one-period averages as pointwise cancellation, promoting the scalar fixture to
a physical phase, reusing the R-166 coefficient-one harmonic lemma rootwise,
and claiming A13/T-050/Sector-A closure. No negative result, tier change, gate
closure or PDF follows.
