# R-188 signed Jensen-defect telescope cross-check

R-188 is a T0 claim-nonbearing Lean cross-check for the R-081 adapted-NEAR
split. For `A=e2(1+(2/5)e1)` and `H(x)=x^2` on two Rademacher roots, the
conditional quadratic values are `J0=29/25`, `J1(-1)=9/25`, `J1(1)=49/25`,
and `J2=A^2`. The first Jensen increments are `-4/5` and `4/5`; the second
increment vanishes. Lean proves the general signed telescope
`dH1+dH2=J2-J0`, the exact secant-plus-defect recombination, zero endpoint
mean, and absolute first-defect mean `4/5>0`. A stdlib-only Fraction lane
reproduces the same values and the integrated verifier rejects eight hostile
mutations.

This is a finite diagnostic, not the A1 production adapted operator. It does
not provide the complete signed NEAR lower bound, root-resolved FAR,
progressive/revisit uniformity, `OVERLAP_src`, controlled-shell one-use,
Nelson, a measure, Sector-A, Pre-A, any limit, or a gate/tier change. The
production map must be inserted before this signed telescope can be used as a
load-bearing owner identity.
