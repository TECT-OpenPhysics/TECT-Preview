# R-176 scope note

This note records the finite A1 covariance instantiation that follows the
supplied-witness interface in R-175. The family-lock mass is reconstructed
from the hash-pinned A1 parameters rather than copied as an executable
constant. At each of `k` and `2k`, the three leading Cholesky pivots are
positive, so the displayed lower factor is defined over the positive real
branch. Its inverse-transpose is an actual finite covariance square-root
witness for the corresponding `C(p)`, and duplication gives the six-real
root used by R-174.

The result is deliberately bounded. It does not select a global measurable
root field or tell us how the heat, root revelation, complement, historical
low, forest, returned mean, source and sextic terms enter a complete scalar
owner. It is therefore a T0 cross-check and not an A13 or Sector-A proof.

The Lean kernel checks the exact Gram template; the primary and independent
lanes check the two A1 roots and their residuals. No claim tier or lifecycle
changes, no new negative result and no PDF follow.
