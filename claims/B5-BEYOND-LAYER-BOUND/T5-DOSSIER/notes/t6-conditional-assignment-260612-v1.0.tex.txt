% =============================================================================
% Title: B5 T6-CONDITIONAL assignment dossier: the T-031 decision enactment (D1-A / D2-A / D3-A)
% Claim: B5-BEYOND-LAYER-BOUND (tier-assignment dossier; enacts the operator verdicts of 2026-06-12)
% Version: v1.0 -- first issued 2026-06-12; this version issued 2026-06-12
% Status: TIER-ASSIGNMENT RECORD, operator-authorized (T-031 verdicts D1-A,
%   D2-A, D3-A, 2026-06-12): B5 re-tiered T5 PINNED-CLOSURE -> T6
%   PROVED-CONDITIONAL on the explicit hypothesis set H_B5^T6; package label
%   B5-BeyondLayer-T6Conditional-260612. NOT T7 (operator boundary: the
%   endpoint is structurally thin and sunset-capped; arbitrary non-lattice
%   exhaustiveness is T-030 frontier). The B1/B2 Reading-H theorem
%   (T7-SCOPE_{C_full} given A1) is unaffected.
%
% Revision history (cumulative):
%   v1.0 (2026-06-12) first issue: enactment record for the T-031 decision
%        dossier verdicts, with the fresh devil's-advocate pass required for
%        the T5 -> T6 promotion.
% =============================================================================

\sloppy
\section*{1. Purpose and scope}

Enactment record for the three T-031 operator verdicts (2026-06-12), with the
promotion-grade devil's-advocate pass. \textbf{Scope:} the B5 claim tier and
its gate bookkeeping. \textbf{Not claimed:} B5 at T7; arbitrary non-lattice
DR-2; any change to B1/B2.

\section*{2. The three verdicts and their enactments}

\textbf{D1-A (H-ADM-COH, lattice class): DISCHARGE re-affirmed and
strengthened.} The 2026-06-08 discharge (R-026 + R-027 + R-028, operator
enacted) is re-affirmed with the 2026-06-12 evidence: R-026 is now
T7-NTstandard (Lemma NT pinned in-bundle) and the R-028 residual (a)
(subpolynomial-$K$ sufficiency, previously asserted) is PINNED by the
exhaustive applied check ($2{,}644{,}976$ sums, worst $0.250$; enumerated
$K_{\rm floor}\le12$). H-ADM-COH remains a NON-LATTICE remainder / modelling
frontier only.

\textbf{D2-A (DR2-SHARE): re-scoped.} The gate moves from
``MOOT@lattice / OPEN@arbitrary'' to RESCOPED-TO-T030-NONLOADBEARING: the
$C_{\rm full}$ head theorem caps $T'\le\lfloor2\pi/\theta_{\min}\rfloor=10$
elementarily (Lemma~2), so arbitrary-$Q$ DR-2 is a frontier strengthening,
not a hole; leaving the gate OPEN invited exactly that misreading.

\textbf{D3-A (tier): B5 $\to$ T6 PROVED-CONDITIONAL} on the explicit set
\[
\mathcal H_{\rm B5}^{\rm T6}=\{
\text{lattice-class H-ADM-COH discharged (D1-A)};\
\text{non-lattice remainder excluded (T-030)};\
\]
\[
\text{SC-SCOPE endpoint thinness accepted};\
\text{sunset-limited joint }\times1.040>1\text{ accepted as sufficient}\}.
\]
Label: \texttt{B5-BeyondLayer-T6Conditional-260612}. NOT T7: the endpoint is
structurally thin (sunset-capped at $\times1.13$) and the non-lattice
exhaustiveness is frontier bookkeeping.

\section*{3. Why T6 is now supportable (the T5-dossier ledger, re-inventoried)}

\begin{center}
\footnotesize
\begin{tabular}{p{0.34\linewidth}p{0.56\linewidth}}
\hline
T5-era weakness & 2026-06-12 state\\
\hline
T4-grade lemmas in the chain & DR-2 T7-NTstandard; STEP-5B rectangle T6;
tadpole gates R-U6-1/2 CLOSED (written proof + 8/8)\\
endpoint $\times2.1$ thin & hardened $\times2.6$ (J-refined); SC-SCOPE joint
$\times1.040$--$\times1.082>1$ certified on $\mathcal W_{\rm SC}$\\
SC-SCOPE marginal & T5 thin-CERTIFIED, published; tadpole struck, sunset-only\\
H-ADM-COH class pin & lattice class discharged (D1-A; residual (a) pinned)\\
\hline
\end{tabular}
\end{center}

\section*{4. Devil's-advocate / self-adversarial review (promotion-grade; CLAUDE.md 6.3)}

\begin{itemize}
\item[($\alpha$)] \emph{``The thinness acceptance is doing tier work: a
$\times1.04$ margin inside a T6 label.''} VALID with mitigation: that is
precisely why the acceptance is a NAMED HYPOTHESIS in
$\mathcal H_{\rm B5}^{\rm T6}$, not a buried judgment -- T6 PROVED-CONDITIONAL
is the tier DEFINED for explicit named-hypothesis closures; removing the
hypothesis (hardening the sunset accounting) is the registered T7 path.
\item[($\beta$)] \emph{``The estimate-grade third-cumulant inflation basis
(GATES SC-SCOPE record) survives -- does it block T6?''} ADDRESSED: the
certified joint ($\times1.040$, Parseval-pinned $R_{\max}=0.385<0.634$)
replaced the estimate-grade pairing basis for the ENDPOINT closure; what
remains estimate-grade is the off-endpoint all-orders FEASIBILITY
($I\le10^{-3}$, $\times3.1$--$\times20.7$), which the hypothesis set does not
rely on. Within $\mathcal W_{\rm SC}$ the chain is certificate-grade.
\item[($\gamma$)] \emph{``D1-A re-affirms a 2026-06-08 discharge -- is the
re-tier double-counting the same evidence?''} DISMISSED: the T5 assignment
(2026-06-06) PRE-DATES the discharge enactment, the lattice T7 confirmation,
the NT pin, the certified endpoint joint, and the tadpole closure -- every row
of the Sec.~3 table is post-T5 evidence; nothing is counted twice.
\end{itemize}

\textbf{Quantitative sanity check (mandatory).} The promotion changes NO
number: margins ($\times55.6/\times8.8/\times2.1$--$2.6$), the certified joint
($\times1.040$--$\times1.082$), the threshold ($1.59\times10^5$ vs
$n_{\rm pack}\approx49$), and the R-028 margins ($\times10.6$--$\times15.8$)
are the published-bundle values; the tier action is a classification of the
EXISTING evidence under the TSv2 definition of T6 (theorem under explicit
named hypotheses, each textbook or separately tracked -- here: two acceptance
hypotheses + two scope facts, all four registered).

\section*{5. Result footer}

{\footnotesize
\begin{verbatim}
Result ID:               B5-BeyondLayer-T6Conditional-260612
                         (tier-assignment dossier, T-031 enactment)
Precise statement:       B5-BEYOND-LAYER-BOUND re-tiered T5
                         PINNED-CLOSURE -> T6 PROVED-CONDITIONAL on
                         H_B5^T6 = {lattice-class H-ADM-COH
                         discharged; non-lattice remainder excluded
                         (T-030); SC-SCOPE endpoint thinness
                         accepted; sunset-limited joint x1.040 > 1
                         accepted}. D1-A: lattice discharge
                         re-affirmed + residual (a) pinned (2.6M
                         exhaustive check). D2-A: DR2-SHARE rescoped
                         to T-030 non-load-bearing frontier.
Scope:                   B5 claim tier + gate bookkeeping only.
                         B1/B2 (T7-SCOPE_{C_full} given A1)
                         unaffected. NOT T7 (endpoint structurally
                         thin, sunset-capped; non-lattice = T-030).
Evidence grade:          TIER-ASSIGNMENT RECORD over published
                         bundles + GATES records; promotion-grade
                         devil's-advocate pass included.
Reproduction command:    the three B5 published bundles' entry
                         manifests (8 + 1 + 4 scripts, 259 asserts).
Falsifier:               any H_B5^T6 element failing (e.g. a
                         recomputed joint <= 1 on W_SC; a lattice
                         competitor violating the discharged
                         G1'''-AE bound).
Tier before / after:     B5: T5 PINNED-CLOSURE -> T6
                         PROVED-CONDITIONAL (operator verdicts
                         D1-A/D2-A/D3-A, 2026-06-12).
No-overclaim statement:  T6-CONDITIONAL, not T7; the acceptance
                         hypotheses are named, not buried; arbitrary
                         non-lattice DR-2 remains OPEN (T-030).
Next required action:    T7 path (optional): harden the sunset
                         endpoint beyond thin OR close T-030.
                         Mainline: T-031 CLOSED as decision layer.
\end{verbatim}
}
