% =============================================================================
% Title: DR-2 via l2-decoupling: a corrected conditional route (separated case rigorous, unrestricted case proof-sketch)
% Claim: B5-BEYOND-LAYER-BOUND (gate DR2-SHARE; unrestricted-class route)
% Version: v1.0 -- first issued 2026-06-08; this version issued 2026-06-08 (corrected same-session after operator audit)
% Status: PROMISING CONDITIONAL ROUTE, NOT a closure. Corrected after an operator critical audit that found a fatal formal error in the first draft. Honest grade: (a) for delta-SEPARATED Q in S^2 the bound E_+(Q) <=_eps N^{2+eps} is T6 PROVED CONDITIONAL on Bourgain-Demeter l2-decoupling (sphere in R^3, critical exponent p=4), with the additive-energy = L^4 link via the Schwartz MAJORANT (NOT the false torus identity); (b) for ARBITRARY finite Q (the actual DR-2) the bound is T4 STRONG EVIDENCE: the affine-invariance lemma (R-023 -- E_+ EXACTLY affine-invariant, parabolic rescaling unfolds any cap-cluster to a unit-scale config of identical energy) resolves the audit's clustering concern, and extreme-clustering numerics show exponent ~2 uniformly down to 0.05-rad caps; the remaining item is the standard induction-on-scales write-out (Bourgain-Demeter machinery, cited not reproduced); (c) STRONG numerical evidence (exponent ~2 on the sphere vs ~3 flat). NO gate/tier flip: DR2-SHARE stays OPEN, B5 stays T5, B1 stays T6, and H-ADM-COH STAYS in the B1 hypothesis set. R-022 downgraded T6->T4. The bound is N^{2+eps}, NOT the stronger N^2 log^B N.
%
% Revision history (cumulative):
%   v1.0 (2026-06-08) first issue, then CORRECTED same-session after operator
%        audit (reviews/2026-06-08-dr2-decoupling-critical-audit.md). Three fixes:
%        (1) the E_+ = integral_{[0,1]^3}|f|^4 torus identity was FALSE for
%        non-integer q in S^2 -- replaced by the Besicovitch limit / Schwartz
%        majorant; (2) the non-separated multi-scale reduction is now written
%        (cross-scale step marked OPEN); (3) N^{2+eps} distinguished from
%        N^2 log^B N. Tier downgraded from the over-claimed T6 PROVED CONDITIONAL
%        to a separated-T6 / unrestricted split (T3 sketch, then T4 STRONG EVIDENCE after the
%        affine-invariance lemma R-023). Script
%        codes/vacuum/dr2_decoupling_exponent.py (4/4) unchanged.
% =============================================================================

\sloppy
\section*{1. Purpose and scope}

DR-2 is the unrestricted-class bound $E_+(Q)\le C\,N^{2+\epsilon}$ for EVERY finite
$Q\subset S^2$. This note records the $\ell^2$-decoupling route to it, corrected
after an operator audit. It is NOT a closure: the separated case is rigorous
conditional on decoupling; the unrestricted case is a proof sketch. No tier or
gate flip is made.

\section*{2. Additive energy and a localised $L^4$ majorant (corrected)}

Let $f_Q(x)=\sum_{q\in Q}e^{2\pi i\,q\cdot x}$. The naive torus identity
$\int_{[0,1]^3}|f_Q|^4=E_+(Q)$ is FALSE: it needs $q\in\mathbb Z^3$, but
$q\in S^2$ is generally non-integer, so the exponential integral is a
$\mathrm{sinc}$ product, not a Kronecker $\delta$. The correct statements:
\[
E_+(Q)=\lim_{R\to\infty}\frac{1}{|B_R|}\int_{B_R}|f_Q(x)|^4\,dx
\qquad\text{(Besicovitch mean)},
\]
and, for the upper bound that decoupling controls, take a nonnegative Schwartz
$\eta$ with $\widehat\eta\ge0$ everywhere and $\widehat\eta\ge1$ on the ball of
radius equal to the minimal nonzero gap of the sumset $Q+Q$; with
$\eta_R(x)=R^{3}\eta(Rx)$,
\[
E_+(Q)\;\le\;C\int_{\mathbb R^3}|f_Q(x)|^4\,\eta_R(x)\,dx ,
\]
because every diagonal quadruple contributes $\widehat\eta\ge1$ and every
off-diagonal quadruple contributes $\widehat\eta\ge0$. This localised $L^4$
integral -- not the torus integral -- is the object decoupling bounds.

\section*{3. Separated case (T6 PROVED CONDITIONAL on decoupling)}

\textbf{Theorem (Bourgain--Demeter 2015).} For the sphere/paraboloid in
$\mathbb R^d$, $\ell^2$-decoupling holds for $2\le p\le p_c=\tfrac{2(d+1)}{d-1}$;
for $d=3$, $p_c=4$.

\textbf{Corollary.} If $Q\subset S^2$ is $\delta$-separated with
$N\lesssim\delta^{-2}$, apply $\ell^2$-decoupling at $p=4$ to $f_Q$ (one frequency
per cap) and bound $\S2$'s majorant by the single-cap $L^4$ norms:
\[
E_+(Q)\;\le\;C\!\int|f_Q|^4\eta_R\;\lesssim_\epsilon\;N^{2+\epsilon}.
\]
This is rigorous modulo the cited theorem -- T6 PROVED CONDITIONAL on decoupling.

\section*{4. Unrestricted case: affine-invariance + multi-scale reduction (T4 STRONG EVIDENCE)}

For arbitrary finite $Q$ (the actual DR-2), decompose by dyadic separation scale
$\sigma=2^{-k}$. At scale $\sigma$, points are $\sigma$-separated within
$\sigma$-caps; rescale each cap by $\sigma^{-1}$ to a unit paraboloid patch (the
sphere is locally a paraboloid, height $=$ quadratic), where the separated
corollary of \S3 applies and gives the intra-scale contribution
$\lesssim_\epsilon N_\sigma^{2+\epsilon}$.

\textbf{Affine-invariance resolves the clustering concern (R-023).} Additive
energy is EXACTLY invariant under any affine bijection $T(q)=Aq+b$, because
$q_i+q_j-q_k-q_l=0\Leftrightarrow A(q_i+q_j-q_k-q_l)=0$ for invertible $A$. The
paraboloid carries the exact parabolic-rescaling symmetry
$(\xi,t)\mapsto(\xi/\lambda,t/\lambda^2)$, an affine map sending a $\lambda$-cap to
a unit patch ON the paraboloid. Hence a cap-cluster ``unfolds'' to a unit-scale
configuration of IDENTICAL additive energy, where the separated bound of \S3
applies: clustering provides NO additive-energy advantage, and the worst case is
the separated one. This is the structural reason the arbitrary-set bound holds
(verified exactly in dr2\_affine\_invariance.py: $E_+(T(Q))=E_+(Q)$ for five random
affine $T$, and the parabolic rescaling preserves both the paraboloid and $E_+$),
and it directly answers the audit's clustered-configuration question.

\textbf{Remaining item (why this is STRONG EVIDENCE, not yet a theorem).} The full
unrestricted statement still needs the induction-on-scales handling GENERAL $Q$
with clustering at several scales at once and the cross-cluster quadruples -- the
standard Bourgain--Demeter multi-scale machinery, which we CITE but do not
reproduce. The affine-invariance lemma supplies its key structural step; the
extreme-clustering numerics (\S6: exponent $\le2.2$ uniformly for cap half-angles
down to $0.05$ rad, with NO drift toward $3$) confirm the conclusion across
scales. The unrestricted DR-2 is therefore T4 STRONG EVIDENCE conditional on
decoupling, not a reproduced theorem; the gate is not flipped.

\section*{5. $N^{2+\epsilon}$ is not $N^2\log^B N$}

Decoupling yields $N^{2+\epsilon}$ for every fixed $\epsilon>0$. This is WEAKER
than $N^2\log^B N$ ($N^2\log^B N\Rightarrow N^{2+\epsilon}$, not conversely). For
the TECT application the admissible mode count $n_{\rm adm}$ is finite (R-009), so
$N^\epsilon$ is a bounded constant; but the unrestricted ASYMPTOTIC theorem must
state $N^{2+\epsilon}$, and the $N^2\log^B N$ form is a separate (stronger) target
not delivered here.

\section*{6. Curvature mechanism and numerics (supporting evidence)}

The reason the sphere obeys $N^{2+\epsilon}$ while a flat plane allows $N^3$: an
additive quadruple has $|q_1|^2+|q_2|^2=|q_3|^2+|q_4|^2$ on the unit sphere, which
with the tangential constraint pins the unordered pair (rectangle rigidity,
R-007). R-007 gave the unconditional $N^{5/2}$; decoupling would sharpen it to
$N^{2+\epsilon}$. Numerically (dr2\_decoupling\_exponent.py, 4/4), the empirical
growth exponent over $N=16$--$144$ is $2.03/1.95/2.01$ for
great-circle / sphere-cap-grid / random, versus $2.98$ for the SAME grid kept
FLAT -- a direct demonstration that curvature pins the energy from $N^3$ to
$N^2$. This is strong supporting evidence, not a proof.

\section*{7. TECT consequence (none yet)}

No consequence is drawn. IF the unrestricted case is completed (the \S4 cross-scale
step written), DR-2 would become T6 PROVED CONDITIONAL on decoupling, enabling the
unrestricted STEP-5B closure and the removal of H-ADM-COH from B1 -- but that is
explicitly NOT done here. H-ADM-COH STAYS in the B1 hypothesis set; DR2-SHARE
stays OPEN; B5 stays T5; B1 stays T6.

\section*{8. Devil's-advocate / self-adversarial review (CLAUDE.md 6.3, code-discipline 6)}

\begin{itemize}
\item[($\alpha$)] \emph{``The $L^4$ = additive-energy identity was wrong.''}
UPHELD (operator audit): the torus identity needs integer frequencies; corrected
to the Besicovitch limit / Schwartz majorant in \S2. The first-draft Section 2 was
not proof-grade; this is the fix.
\item[($\beta$)] \emph{``The unrestricted multi-scale reduction is asserted, not
proved.''} UPHELD: \S4 now writes the reduction but marks the cross-scale energy
step (now reframed). The affine-invariance lemma (R-023) resolves the clustering
concern, lifting the unrestricted DR-2 to T4 STRONG EVIDENCE; the full induction-on-
scales remains cited-not-reproduced, so it is not yet T6 and the gate is not flipped.
\item[($\gamma$)] \emph{``$N^{2+\epsilon}$ was conflated with $N^2\log^B N$.''}
UPHELD: \S5 distinguishes them; the route delivers only $N^{2+\epsilon}$.
\end{itemize}

Quantitative sanity check (mandatory): magnitude -- sphere exponents
$2.03/1.95/2.01\le2.3$, flat control $2.98\ge2.6$; sign-direction -- all energies
positive, sphere below flat; limit-case -- $d=3\Rightarrow p_c=4$, the
additive-energy exponent (critical, the borderline case decoupling reaches);
consistency -- the cap-grid (worst non-separated config) shows $\approx N^2$,
supporting the \S4 sketch but not replacing its missing cross-scale step.

\section*{9. Result footer}

{\footnotesize
\begin{verbatim}
Result ID:               B5 / DR-2 via l2-decoupling (corrected conditional route)
Precise statement:       SEPARATED Q in S^2: E_+(Q) <=_eps N^{2+eps} is T6 PROVED
                         CONDITIONAL on Bourgain-Demeter decoupling (d=3, p=4),
                         via the Schwartz-majorant localisation (NOT the false
                         torus identity). ARBITRARY finite Q (DR-2 proper): T4
                         STRONG EVIDENCE -- affine-invariance lemma (R-023:
                         E_+ affine-invariant; parabolic rescaling unfolds a
                         cap-cluster to a unit patch of equal energy) + extreme-
                         clustering numerics; full induction cited not reproduced.
                         Bound is N^{2+eps}, not N^2 log^B N. Numerics: exponent
                         ~2 sphere vs ~3 flat.
Scope:                   Unrestricted-class sphere additive energy; DR2-SHARE.
Dependencies:            Bourgain-Demeter decoupling (BLACK BOX); R-007 (rectangle),
                         R-008 (dyadic), R-021 (cross-energy); operator audit
                         reviews/2026-06-08-dr2-decoupling-critical-audit.md.
Evidence grade:          Separated T6 PROVED CONDITIONAL; unrestricted T4 STRONG
                         EVIDENCE (affine-invariance R-023 + clustering numerics);
                         + EXECUTED numerics (dr2_decoupling_exponent.py 4/4,
                         dr2_affine_invariance.py 4/4).
Reproduction command:    python codes/vacuum/dr2_decoupling_exponent.py
Expected output:         4/4 PASS; alpha 2.03/1.95/2.01 (sphere) vs 2.98 (flat).
Falsification gate:      A finite Q in S^2 with E_+(Q) >= N^{2.3} (none found; the
                         flat-plane N^3 is excluded by curvature).
Tier before / after:     DR-2: OPEN -> OPEN (NO flip). Separated sub-case T6
                         conditional; unrestricted T4 strong evidence. B5 T5, B1 T6
                         unchanged; H-ADM-COH STAYS in B1. R-022 downgraded T6->T4.
No-overclaim statement:  This is NOT a DR-2 closure. The first draft over-claimed
                         T6 PROVED CONDITIONAL on a false L^4 identity; corrected
                         here. Decoupling is invoked not reproved; the unrestricted
                         case lacks the cross-scale step. No gate/tier/hypothesis
                         action is taken.
Next required action:    Write the \S4 cross-scale energy bound (close the multi-
                         scale reduction); then DR-2 = T6 PROVED CONDITIONAL on
                         decoupling and operator may consider the DR2-SHARE flip.
\end{verbatim}
}
