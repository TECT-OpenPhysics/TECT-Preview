% =============================================================================
% Title: STEP-5B exhaustiveness decision support: the T-031 operator-decision dossier
% Claim: B5-BEYOND-LAYER-BOUND (T-031 decision layer; DECISION-SUPPORT ONLY -- enacts nothing)
% Version: v1.0 -- first issued 2026-06-12; this version issued 2026-06-12
% Status: DECISION-SUPPORT NOTE. This document enacts NO gate flip, NO tier
%   change, NO scope change. It enumerates the open operator-decision items of
%   the STEP-5B exhaustiveness layer (T-031), assembles the evidence state of
%   each AFTER the 2026-06-12 closures (DR-2 T7-NTstandard with the Lemma NT
%   in-bundle pin; R-U6-1/R-U6-2 discharged; all five main-line bundles
%   PUBLISHED; synthesis layer freeze-ready), and pre-formulates the decision
%   options with their ledger effects, so each decision is a single verdict.
%
% Revision history (cumulative):
%   v1.0 (2026-06-12) first issue, immediately after the R-U6-x discharge
%        unblocked T-031 (the named backlog input).
% =============================================================================

\sloppy
\section*{1. Purpose and scope}

T-031 is the remaining DECISION layer of STEP-5B: the budget COMPARISON is
machine-closed and the analytic inputs are published; what remains are
operator rulings on class definitions and gate/tier consequences. This note is
the dossier for those rulings. \textbf{Scope:} enumeration + evidence state +
options. \textbf{Not claimed:} no new mathematics; nothing is enacted here.

\section*{2. What is already closed (no decision needed)}

(i) Budget comparison: $K(n_{\rm pack})\le113$ (rectangle bound) vs measured
$K=107$, against $K_{\rm budget}=5972/927/221$ -- margins
$\times55.6/\times8.8/\times2.1$ ($\times2.6$ with the J-refined endpoint);
official threshold $n_{\rm adm}\lesssim1.59\times10^5$ vs $n_{\rm pack}\approx
44$--$49$ ($\times3200$). (ii) The rectangle prefactor: PUBLISHED
(\texttt{STEP-5B-Rectangle-T6-260612}). (iii) Lattice-class additive energy:
PUBLISHED, T7 CONFIRMED (\texttt{DR2-Lattice-T7-NTstandard-260612}; Lemma NT
pinned in-bundle, $2{,}644{,}976$-sum exhaustive applied check). (iv) The
tadpole channel: gates R-U6-1/R-U6-2 DISCHARGED 2026-06-12 -- the
third-cumulant threat is SUNSET-ONLY, and the endpoint joint is certified
$\times1.040$--$\times1.082>1$ on $\mathcal W_{\rm SC}$
(\texttt{SC-SCOPE-T5-260612}). (v) RES-4 layer ratio $\rho(I)\ge2.58$
(monotone, 201 nodes).

\section*{3. Decision item D1: H-ADM-COH discharge for the lattice class}

\textbf{Question.} Is the admissibility hypothesis H-ADM-COH (coherence-derived
competitor class: registered shells/shell-unions, $\theta_{\min}$ separation,
$n\le n_{\rm pack}$) DISCHARGED -- i.e.\ no longer a hypothesis but a derived
constraint -- for the lattice competitor class?

\textbf{Evidence state (per GATES.md R-028 + the 2026-06-12 closures).}
R-027 (weighted Lemma A, T7) + R-026 (lattice $T'\ll_\varepsilon R^\varepsilon$,
now T7-NTstandard with the NT pin in-bundle) give the weighted additive-energy
bound with $K\sim R^\varepsilon$ for the unrestricted LATTICE class WITHOUT the
separation input; the finite margin $K_{\rm adm}=1+T'(Q)\le8+4\sqrt{14}\sqrt n$
is machine-verified at $\times10.6$--$\times15.8$ and growing. What R-028 left
as the discharge residuals: (a) the subpolynomial-$K$ sufficiency over the
admissible $R$-range -- NOW PINNED (the exhaustive 2.6M-sum check bounds every
applied instance; enumerated competitors close exactly at $K_{\rm floor}\le12$);
(b) the modeling residual (carrier-richness $\chi(P)\lesssim T'$ link) --
unchanged, an operator judgment by design.

\textbf{Options.}
\begin{itemize}
\item[D1-A.] DISCHARGE for the lattice class (H-ADM-COH becomes a derived
constraint there; the amended-class qualifier in B5's scope narrows to the
non-lattice remainder). Ledger effect: B5 scope string updated; B1 unaffected
(rests on H-LAYER alone); DR2-SHARE handling moves to D2.
\item[D1-B.] HOLD (status quo): H-ADM-COH stays a named class definition.
No ledger change; T-031 stays open on this item.
\end{itemize}

\section*{4. Decision item D2: the DR2-SHARE gate}

\textbf{Question.} GATES.md carries DR2-SHARE as OPEN ``pending operator
discharge'' (the unrestricted/arbitrary-$Q$ additive-energy share). With the
synthesis-layer finding that arbitrary-$Q$ DR-2 is NOT load-bearing for the
$C_{\rm full}$ theorem (Lemma~2 caps $T'\le10$ in-class) and the lattice class
now T7-confirmed, should DR2-SHARE be (A) re-scoped to the explicitly
non-load-bearing frontier track T-030, or (B) kept OPEN as-is?

\textbf{Note.} Option A is bookkeeping (the mathematical content of T-030 is
unchanged); it prevents the OPEN gate from being misread as a hole in the
published theorem.

\section*{5. Decision item D3: the B5 claim tier}

\textbf{Question.} B5 is T5 PINNED-CLOSURE. The T5-DOSSIER's original
``below T6'' rationale cited: (i) T4-grade lemmas in the chain; (ii) the
endpoint at $\times2.1$ (now hardened $\times2.6$); (iii) SC-SCOPE marginal
(now LIFTED@THIN-CERTIFIED with the certified $\times1.040$ joint and the
tadpole struck). Post-2026-06-12, the surviving sub-T6 elements are: the
endpoint thinness (structural, sunset-capped); the H-ADM-COH class pin (D1);
and the estimate-grade third-cumulant inflation basis noted in GATES.

\textbf{Options.}
\begin{itemize}
\item[D3-A.] Re-tier B5 to T6 PROVED CONDITIONAL on the explicit hypothesis
set $\{$H-ADM-COH (if D1-B) or non-lattice remainder (if D1-A); endpoint
thinness acceptance$\}$. Requires: a fresh devil's-advocate pass on the claim
card + the atomic flip set.
\item[D3-B.] Stay T5 PINNED-CLOSURE (status quo) until the sunset endpoint is
hardened beyond thin (e.g.\ a proved $K_{\rm floor}\le0.52\,T'$ floor or a
tighter sunset accounting). No ledger change.
\end{itemize}
The synthesis layer's anti-tier-shopping rule is unaffected by either choice
(units keep their own tiers).

\section*{6. What NO decision here changes}

The published main theorem (T7-SCOPE$_{C_{\rm full}}$ given A1, window
$\mathcal R$) and the B1/B2 canonical states are carried by the head package
and are INDEPENDENT of D1--D3; the five published bundles and the synthesis
layer are frozen artifacts. D1--D3 affect only B5's own scope string, the
GATES.md rows, and the claim-tier bookkeeping.

\section*{7. Devil's-advocate / self-adversarial review (CLAUDE.md 6.3)}

\begin{itemize}
\item[($\alpha$)] \emph{``A decision dossier can frame options to favor
promotion.''} ADDRESSED: every option includes the status-quo branch with
``no ledger change''; the D3 surviving sub-T6 elements are enumerated from the
original dossier's own rationale, not re-argued; nothing is enacted.
\item[($\beta$)] \emph{``The D1 residual (b) ($\chi(P)\lesssim T'$) might be
load-bearing, not a judgment.''} VALID with mitigation: per the B5 record
(R-026/R-027 verdicts) this link was explicitly classified an
OPERATOR-DECISION; the dossier preserves that classification and does not
smuggle it into the evidence column.
\item[($\gamma$)] \emph{``Quoting the hardened endpoint ($\times2.6$) without
its conditions.''} DISMISSED: the J-refinement conditions (conservative
criterion band) are in the published STEP-5B bundle's expected outputs; the
conservative end $\times2.1$ is quoted alongside throughout.
\end{itemize}

\textbf{Quantitative sanity check (mandatory).} All numbers quoted here are
published-bundle values (margins $\times55.6/\times8.8/\times2.1$--$2.6$;
$K\le113$ vs $107$; $\times10.6$--$\times15.8$ R-028 margins; joint
$\times1.040$--$\times1.082$; $\rho\ge2.58$; threshold $1.59\times10^5$ vs
$n_{\rm pack}\approx49$); no new computation enters a decision input.

\section*{8. Result footer}

{\footnotesize
\begin{verbatim}
Result ID:               B5-BEYOND-LAYER-BOUND / STEP-5B
                         exhaustiveness decision support (T-031)
Precise statement:       DECISION DOSSIER: three operator items --
                         D1 H-ADM-COH lattice-class discharge
                         (evidence: R-026 T7-NTstandard + R-027 +
                         R-028 margins x10.6-15.8 + 2.6M exhaustive
                         check; residual (b) chi(P)<~T' stays a
                         judgment); D2 DR2-SHARE re-scope to the
                         non-load-bearing frontier (bookkeeping);
                         D3 B5 tier (T6-conditional re-tier vs stay
                         T5). Nothing enacted.
Scope:                   decision layer only; no new math, no flips.
Evidence grade:          DOSSIER over published bundles + GATES.md
                         records.
Reproduction command:    none new (cites published bundle manifests).
Falsifier:               any quoted number diverging from its bundle
                         source.
Tier before / after:     no change (B5 stays T5 pending D3).
No-overclaim statement:  options are presented with status-quo
                         branches; the operator-decision
                         classifications of the B5 record are
                         preserved unchanged.
Next required action:    operator verdicts on D1 / D2 / D3 (each a
                         single ruling); then enact the chosen
                         options as an atomic flip set.
\end{verbatim}
}
