# A2 / R-157 / R-158 submission-preparation package

Manuscript v0.1.42; distribution s1; 9 September 2026.
Source checkpoint: `903426f3229ff1a5bda3dbe961d37bc13c0a5dd0`.

**MATERIALS READY. External mathematical and specialist novelty reviews
NOT PERFORMED. Actual submission NOT PERFORMED.** No review signature is
required to assemble this author-requested package. No external signature,
proof acceptance, priority certification or journal acceptance is simulated.

## Start here

1. Read `paper/manuscript.pdf` (18 pages) and `paper/submission-notes.pdf`
   (2 pages). Their TeX sources and bibliography are included.
2. `paper/cover-letter.md` is an unsent, venue-neutral editor letter. Its
   final operational paragraph must be removed only after the author confirms
   the personal declarations listed there.
3. `paper/submission-readiness.md` lists the remaining author/venue decisions.
   `paper/proof-audit.md`, `paper/theorem-applicability-audit.md`,
   `paper/literature-crosswalk.md` and `paper/rereview-2026-09-09.md` explain
   the internal proof, source and comparison boundaries. Review forms are blank.
4. Current distribution evidence is in `checks/`; paper-local historical
   records keep their original source versions and dates. A PASS is a tooling
   or finite-check result, not a count of analytically proved propositions.

## Exact scientific scope

This is the explicitly printed regularized classical functional for three
complex/six real components on the fixed side-16 periodic torus. The three
results concern global gradient-flow well-posedness, exact neutral rejection
and decay, and a separately imposed charge/chemical-potential ensemble with
shell coexistence. The v0.1.42 mathematics, limitations, appendix and
bibliography equal v0.1.41 after newline decoding. R-472 is assurance-only.
No physical vacuum, derived physical charge, BCC selection, quantum or
infinite-volume limit, cosmology or Sector-A closure is claimed. The canonical
Laplacian shorthand remains a transfer-only issue, not a hidden premise of
the explicitly defined standalone functional.

## Verify the delivery

The distribution ZIP is `v0142-s1.zip`. From the extracted package, run:

```text
python -X utf8 verify-delivery.py --self-test --check /path/to/v0142-s1.zip
```

`TRANSPORT-MANIFEST.json` inventories every other file by size and SHA-256.
It records the separate source archive identity. These are integrity checks,
not cryptographic author signatures: retain a trusted copy of the outer ZIP
hash when transmitting. The original transport manifest and fixed-commit
clean replay remain under `checks/` rather than being relabelled as new runs.

## Full reproducibility companion

`a2-r157-r158-reproduction-source-v0.1.42.zip` is the separate full committed
source archive. It is larger because existing integrated checks depend on the
wider historical registry. Unrelated contents are context, not paper claims.
It contains no Git metadata, private working directory or runtime caches.
Its exact identity is recorded in the delivery manifest; the large binary
is deliberately not committed a second time to the Git repository.

Verify it with the same tool before extraction:

```text
python -X utf8 verify-delivery.py --check /path/to/a2-r157-r158-reproduction-source-v0.1.42.zip
```

Extract the companion into a new directory and run from its `source/`:

```text
python -X utf8 publish/papers/a2-r157-r158-ensemble-minimizers/verification/replay_archived_source.py --self-test --lean-cache /path/to/resolved/.lake --tectonic /path/to/tectonic --output /path/to/new-replay.json
```

No Git installation is needed for this extracted-source replay. Python and
the source-declared SymPy, Lean/Mathlib and historical Tectonic environment
remain explicit external dependencies. Do not replace old expected hashes
when a dependency or compiler version differs. The new delivery replay
uses Tectonic 0.16.9 for the historical note-byte contract; the finished
manuscript was built with Tectonic 0.17.0. All original paper/PDF bytes are
preserved. Earlier sandbox runtime-access failures are retained separately;
they executed no mathematical checks and are not mathematical failures.

The full archive can be regenerated by the pinned source checkpoint's
`verification/submission_package.py` export workflow after its exact-commit
clean replay. Its original small transport archive is an input, not this
new distribution. No commit, integration or remote backup is implied by
the existence of an archive alone.

## Author-controlled next step

Select the venue, confirm affiliation/contact, funding, conflicts,
contributions, prior dissemination and exclusivity facts, approve the cover
letter and final manuscript, and check that venue's actual current upload
requirements. Actual uploading/sending and the repository submission tag
require separate instruction. External review remains welcome but is not
a prerequisite for completing the present materials package.
