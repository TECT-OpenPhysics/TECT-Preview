# codes/ — numerical codes by domain

`vacuum/ topology/ gravity/ flavor/ cosmology/ common/`. Module docstrings
state purpose, claim IDs served, and reproduction role. Code changes that
affect a claim's evidence update the claim card in the same commit. Evidence
runs write JSON artefacts under `claims/<ID>/runs/` per
`governance/verification-standard.md` §4.
