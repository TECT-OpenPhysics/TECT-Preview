# TECT — Python dependencies for the verification chain and numerical codes.
# Install:  pip install -r requirements.txt
#
# Numerical and PDF-verification dependencies used by the fail-closed research
# workflow. The TeX engine itself is a separate executable; see SESSION.md.
numpy>=1.24
sympy==1.14.0
python-flint==0.9.0
pypdf>=5.0
pdfplumber>=0.11
reportlab>=4.0
pytest>=8.0

# Not installed by pip (required for rebuilding proof-note PDFs):
#   - `tectonic` beside the active venv's Python executable; or
#   - a TeX distribution providing `pdflatex` (e.g. TeX Live / MiKTeX).
# `doctor.py` reports NOT READY when neither engine is available.
