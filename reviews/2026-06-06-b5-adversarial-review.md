# Operator adversarial review — B5 / Sector-B documents (2026-06-06)

**Reviewer**: operator (Jusang Lee). **Mode**: adversarial / attack-points only.
**Binding outcome**: tiers FROZEN (no promotion this review); scope reinforcements
required; SC-SCOPE all-orders lift OPEN pending four named inputs; DR-2 off the
critical path. This file is the canonical archive of the review (CLAUDE.md §6.3.5).

## Official status to hold (operator)

- **B5 = T5 PINNED-CLOSURE only under H-ADM-COH + second-cumulant scope.** The
  scope string must read `CLOSED@H-ADM-COH-AMENDED-CLASS`; any reading as an
  unrestricted claim is an immediate overclaim. T6 forbidden now.
- **SC-SCOPE lift = OPEN** until M-ENDPOINT, Ĝ³(q₀), per-transfer Ĝ⁴(|t|), and
  the tadpole formal alignment (R-U6-1) are all completed.
- **DR-2 = research branch / off critical path.** DR2-SHARE stays the formal
  obstruction.
- Separate, in any external-facing text, "executed closure package" (192/192
  reproducibility within the pinned scope) from "physical theorem closure"
  (which class-amendment + SC-SCOPE removal would require).

## Danger ranking (most → least)

1. quartic-difference endpoint marginal (×1.0)
2. third-cumulant endpoint lift
3. tadpole formal alignment
4. DR2-SHARE
5. T5 scope overclaim
6. dyadic constant optimisation

## Per-document attack points + required reinforcements

### 1. t5-assignment-dossier v2.0 — T5 OK, "closure" wording risky
- (a) H-ADM-COH is a class amendment, not a proven law; external reviewer will
  say "won by shrinking the class". Keep the scope string only.
- (b) endpoint margin ×2.6 too thin; third-cumulant endpoint marginal ⇒ cannot
  go T6. The note's own "below T6" is correct.
- (c) 192/192 = executed reproducibility within pinned scope, NOT a physical
  unconditional proof; separate the two in external text.
- **Required**: keep T5, forbid T6, fix "not unrestricted / not all-orders"
  prominently on the first page.

### 2. dyadic-lift-log-sharpening v1.0 — real improvement, not closure-critical
- (a) the gain is only log²(2n) → log^{3/2}(2n); does NOT remove the √n growth,
  so still insufficient for unrestricted fixed-budget closure.
- (b) architecture still Minkowski-then-4th-power; ℓ² assembly / TT* may be
  better — current is best-within-architecture, not the structural optimum.
- (c) the 8→4 candidate must NEVER enter the ledger before a separate machine
  verdict (catch-#7-class slip risk).
- **Required**: official bound only to log^{3/2}; 8→4 banned from the ledger.

### 3. quartic-difference-channel v1.0 — most dangerous for the SC-SCOPE lift
- (a) at sup-kernel grade the quartic-difference channel is a bigger threat than
  the cubic sunset; at the endpoint its ×2.6 inflation eats almost the whole
  ×2.6 margin ⇒ UNDETERMINED without per-transfer refinement, not merely marginal.
- (b) need a topology audit that Lemma-H sextic fold and the quartic-difference
  loop are truly disjoint (else double-count, or removing overlap could improve
  the verdict). Currently estimate-grade.
- (c) the t=0 reabsorption depends on U6 normal-ordering; if U6 is a T3 sketch
  this note cannot rise above it.
- **Required**: no endpoint closure without per-transfer Ĝ⁴(|t|); this is
  "inventory complete", NOT "third-order lift closed".

### 4. dr2-extraction-lemmas v1.0 — L1/L2 good, NOT DR-2 closure
- (a) L1 diametral-disjointness and L2 extraction decomposition look exact, but
  L3 is conditional and its ℓ¹-controlled hetero bound is schematic (not yet a
  usable sharp inequality).
- (b) DR2-SHARE is the real bottleneck: adjoining rich circles breaks
  Σ_s I_{C_s}² ≤ I² via point-sharing; without it, extraction cannot keep the
  homo cap.
- (c) "elementary route closes iff χ(P)=O(polylog)" is useful but NOT equivalent
  to all of DR-2 (it is the extraction-route sticking point, not all
  polynomial/incidence methods).
- **Required**: keep DR2-SHARE as the formal obstruction; DR-2 stays an
  off-critical research branch.

### 5. sunset-endpoint-refinement v1.0 — direction right, needs M-ENDPOINT
- (a) re-evaluating u_eff at the endpoint dressing is valid, but the ×1.34
  endpoint ratio is a linear-response estimate; M is convex ⇒ not a proven
  floor. M(0.33675) machine evaluation is the named missing constant — without
  it this is "candidate removal", not "removed endpoint failure".
- (b) the kernel-axis "low-yield" call is reasonable but not quantitative;
  Ĝ³(q₀) is not actually computed.
- (c) adding the quartic-difference channel makes the endpoint marginal again.
- **Required**: compute M-ENDPOINT; optional Ĝ³(q₀); re-assemble as a JOINT
  inequality with the quartic channel.

### 6. tadpole-reabsorption-lemma v1.0 — key cancellation, still a sketch
- (a) normal-ordering removing the tadpole is physically natural, but external
  reviewer: "chose bookkeeping to kill a dangerous channel?" — weak without a
  formal alignment write-up.
- (b) must close whether competitors are each evaluated at their own
  stationarity point; the off-optimum residual linear term absorbed into an
  in-layer move needs a formal proof.
- (c) O(F³) remainder resonant triples can make O(I²) cross terms; the claim
  that this is already in the production N₄ resonance accounting must be closed
  in R-U6-1 with equations.
- **Required**: no tier above T3 without R-U6-1 (formal alignment) + R-U6-2
  (coefficient script). "tadpole absent" is a matched-bookkeeping theorem
  CANDIDATE, not a T6 lemma.

### 7. third-cumulant-lift-assessment v1.0 — a danger map, not a lift
- (a) the cubic sunset is one-sided dangerous (lowers competitors, absent for
  Reading-H) ⇒ a direct threat to selection stability; δF₃ ≤ 0 handled correctly.
- (b) endpoint sunset ratio ×0.97 and tadpole-if-uncancelled ×0.53 are serious:
  the lift FAILS at sup-bound grade ⇒ per-transfer G³, cubic-transfer counting,
  and tadpole cancellation are all load-bearing.
- (c) the dressed coupling u_eff = u + 10vM's Wick coefficient is estimate-grade
  until machine/symbolic verification; all third-cumulant docs must use the
  dressed coupling only (bare coupling = optimistic error).
- (d) do not confuse composed-margin vs full-floor comparison: U1 theorem uses
  the composed margin; full-floor only for a new joint 2nd+3rd inequality.
- **Required**: keep at T2 estimate; do not call the endpoint failure merely a
  "sup-kernel artifact" — keep "three named lift inputs required" explicit.

### 8. hlayer-residual-inventory v1.0 — useful, "4/6 covered" risky
- (a) RES-1 off-diagonal Bloch coverage can conflate the trial-class problem
  with the correction-order problem; full-covariance Gaussian trial lowering the
  layer floor is RES-5. The merger is noted but a reviewer will probe it.
- (b) RES-4 "mechanical" undersells it: the interval sweep ties J integrals, K
  budgets, packing counts, intensity-dependent dressing — "mechanical-but-
  nontrivial".
- (c) biggest risk = status inflation: RES-3 and RES-5 are genuine open frontier
  ⇒ unconditional whole-Reading-H is NOT yet here; the inventory is an
  attack-surface enumeration, not a closure claim.
- **Required**: keep RES-3/RES-4/RES-5 as independent open axes; "covered" only
  as "conditionally covered at amended-class, second-cumulant, three-anchor
  scope".

## Final recommendation (operator)

Now is NOT the time to raise tiers. The decision to make is whether to close the
endpoint third-order lift by a per-transfer machine audit (M-ENDPOINT + Ĝ³ + Ĝ⁴
+ tadpole alignment).

## Actions taken (this commit)

- Named lift inputs registered as gates: M-ENDPOINT, GHAT3-Q0, GHAT4-PERTRANSFER,
  R-U6-1, R-U6-2, DR2-SHARE. SC-SCOPE row tightened to enumerate the lift inputs.
- B5 card scope: "not unrestricted / not all-orders" reaffirmed prominently
  (no tier change). B1 card SC-SCOPE note enumerates the four lift inputs.
- t5-assignment-dossier re-issued with the prominent first-section scope banner
  and the executed-package vs physical-closure separation.
- The seven U-series drafts retain their honest tiers (T2/T3/survey); their
  per-document reinforcements above are registered here and will be applied when
  each is next advanced or graduated to a proof note. No tier raised anywhere.
