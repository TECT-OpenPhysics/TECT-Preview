# Operator adversarial review — t5-dossier v2.1 / ha0-removal v2.1 / ga0-dui v1.1 (2026-06-06)

**Reviewer**: operator. **Verdict**: the prior reinforcements are well-applied; the three documents are now near-defensible. Remaining instruction: the master ledger MUST show the SUPERSESSION CHAINS, and outdated snapshots (esp. ga0-dui v1.1's "ROBUSTNESS-MU2 open" footer) must not be repeated as current status. No tier raised. Canonical archive (CLAUDE.md §6.3.5).

## What was fixed well (operator)

- B5 scope fence hardened (CLOSED@H-ADM-COH-AMENDED-CLASS; not unrestricted / not all-orders).
- H-A0 replacement vs full-discharge separated (ha0-removal = replacement; ga0-dui = completion).
- G-A0-DUI two-sided dominated convergence + three-region domination.
- anchor-only / not-global-H-LAYER made explicit.

## Remaining attack priorities (operator)

H-ADM-COH remains a class amendment > SC-SCOPE all-orders still open > B5 internal links include T4-grade pieces (AddC) > anchor verified facts still depend on A1-KERNEL-CONV > ROBUSTNESS-MU2 supersession must be tracked.

## t5-dossier v2.1 — attack points

1. "CLOSED" still strong for external readers: external text must say "STEP-5B is closed within the pinned H-ADM-COH amended class, matched second-cumulant scope", never standalone "STEP-5B is closed".
2. H-ADM-COH is a class amendment, not a proven law: B5 = "operationally closed under the adopted admissibility rule", NOT "proves physical selection against all mathematical patterns".
3. T6 forbidden for three live reasons: (a) some internal links T4-grade (AddC), (b) endpoint margin only x2.6, (c) third-cumulant endpoint marginal. 192/192 = executed reproducibility package within pinned scope, NOT theorem-grade unconditional proof.
4. Tier logic (IMPORTANT): "B1 T6 consumes B5 T5" must be framed as B1 consuming B5/STEP-5B as a NAMED HYPOTHESIS (H-ADM-COH / scope pin), NOT as a proved unconditional theorem. B1 T6 statement structure: "IF H-ADM-COH and SC-SCOPE hold, then Reading-H is selected." Consuming B5 as a closed theorem revives the tier-monotonicity attack.
5. AddC indistinguishability lemma is T4 ⇒ it is the weakest internal link; a closure chain containing a T4-grade indistinguishability cannot be promoted to clean T6. T5 is correct now.
6. third-cumulant marginality, though outside B5's second-cumulant scope, will be read by physicists as an all-orders stability question: introduce B5 as a "second-cumulant selection bound, not all-order stability".

## ha0-removal v2.1 — attack points

1. v2.1 alone is replacement (H-A0 → H-ANCHOR + G-A0-DUI), NOT full discharge; cite only WITH ga0-dui v1.1.
2. production-anchor-only is the binding limit; not a parameter-robust theorem by itself.
3. M'<0 depends on G-A0-DUI, so v2.1's sign theorem is conditional on H-ANCHOR + G-A0-DUI; G-A0-VER 14/14 does not make L1's regularity unconditional in v2.1 alone.
4. m_c=19.96 clarified as monotone threshold; in a paper, present it as an auxiliary monotone threshold with no physical interpretation.
5. anchor constants provenance remains under A1-KERNEL-CONV; keep m*>m_w, M_R>M_c as VERIFIED ANCHOR DEPENDENCIES in GATES (deleting the H-ANCHOR row = dependency laundering).
6. A=0 section only; forbidden "H-LAYER fully solved"; A≠0, anisotropic, off-diagonal covariance, matched-order corrections untouched.

## ga0-dui v1.1 — attack points

1. DUI core attacks largely removed (two-sided + three-region). Strong now.
2. keep the M' measure convention distinct from the J-normalization (the M'=-J(0) vs -J(0)/2 factor-2 was a different document's error).
3. "A=0 uniqueness unconditional" is anchor-only; the qualifier must never drop in downstream ledger.
4. B2/B1 hypothesis shrink is a re-classification of a named hypothesis to a verified dependency INSIDE the already-T6 card, NOT a promotion by this T4 note; state this in GATES + cards.
5. "verified fact" ≠ "assumption-free": verified under A1-KERNEL-CONV + gap equation at mu^2=0.005; keep the H-ANCHOR verified-fact row.
6. 23/23 PASS = proof AUDIT (quadrature), carrier = dominated convergence; keep the distinction downstream.
7. the v1.1 footer "ROBUSTNESS-MU2 open and unchanged" is a writing-time snapshot; it was later updated by robustness-mu2-step5b-remargin to a scoped closure CLOSED@[x0.5,x2]-2ND-CUMULANT. Current-status cards must NOT repeat the v1.1 "open" footer verbatim.

## Reconciled official status (master ledger)

- B5 = T5 PINNED-CLOSURE only under H-ADM-COH amended class + second-cumulant scope.
- H-A0 = REPLACED by H-ANCHOR + G-A0-DUI (ha0-removal v2.1); G-A0-DUI = CLOSED (ga0-dui v1.1); H-ANCHOR = DEMOTED to VERIFIED ANCHOR FACT (under A1-KERNEL-CONV at mu^2=0.005).
- A=0 uniqueness = UNCONDITIONAL AT THE PRODUCTION ANCHOR.
- H-LAYER = NOT globally closed by these notes.
- ROBUSTNESS-MU2 = scoped closure CLOSED@[x0.5,x2]-2ND-CUMULANT (supersedes the ga0-dui v1.1 "open" snapshot).

## Actions taken (this commit)

- Review archived; supersession-chain instruction recorded.
- B1 card: tier-logic made explicit -- B5/STEP-5B is consumed as the NAMED HYPOTHESIS H-ADM-COH (scope pin), not as a proved unconditional theorem (TSv2 §4.4); B1 T6 = "IF {H-LAYER, H-ADM-COH, SC-SCOPE} then Reading-H selected at second-cumulant scope".
- ga0-dui v1.1 + ha0-removal v2.1: STATUS-UPDATE annotation that their ROBUSTNESS-MU2 "open" footers are superseded by the scoped closure; live status is the cards/GATES, not the note snapshots (canonical-source hierarchy).
- README B5 framing checked clean (no standalone "STEP-5B is closed"). No tier raised.
