# N-001 q1a BCC-bridge evidence package

**Status:** FROZEN EXPERIMENTAL RECORD.  This is not a claim reproduction
bundle and it has no tier.  It preserves the evidence boundary needed to
resume this question without repeating historical discovery work.

## Read this first

`MANIFEST.json` is the machine-readable authority.  `SHA256SUMS.txt` locks the
nine repository evidence files.  The parent freeze record explains the
interpretation, and `RUNBOOK.md` gives integrity and rerun procedures.

Evidence lock digest (SHA-256 over the exact `SHA256SUMS.txt` lines):

```text
ecb48ed407a9eecce757554f37a8ccadf541a0f54253521ba0d037d6f04af766
```

## Settled record

- A nonzero homogeneous condensate candidate was recorded at N32/N64/N128.
- Its structure audit is `NONUNIFORM_BUT_NO_Q0_SHELL`, not BCC.
- The complete N32 commensurate BCC `{110}` star has positive recorded
  projected curvature: minimum `+52.12042392718455`; six of six antipodal
  pairs were evaluated; no negative or near-zero direction was recorded.
- The stored BCC-seed sweep is a negative result because it did not retain
  q0-shell BCC modulation.

These observations are diagnostic only.  They do not establish a global
minimum, full-spectrum stability, BCC existence/nonexistence, Reading-H
selection, a new claim, or a tier change.

## Evidence authority and portability

The package is fully checkable inside a TECT checkout: every locked evidence
file is tracked here.  Direct recomputation of the BCC-star probe additionally
requires the historical external run root `C:\Dev\Runs\q1a_final_pubgrade_compat_v2\refinement`,
the external PDE source tree `C:\Dev\Codes\PDE`, and a Python environment with
NumPy and Torch.  The original uploaded condensate runs likewise require their
external source and run artefacts.  Hashes for those external dependencies are
preserved in the two JSON evidence records; they are not silently represented
as self-contained repository artefacts.

The scoped A1 T5 manifest is an upstream convention anchor only.  It does not
certify this numerical diagnostic and this diagnostic does not enlarge A1's
scope.

## Restart boundary

No further work is required to preserve this record.  Reopen only with a new
question and a new evidence record: a declared N64/N128 transfer audit, a new
parameter/operator regime, or a field that passes the q0/BCC structure audit.
Never overwrite the locked evidence when doing so.
