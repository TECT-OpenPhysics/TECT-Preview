# EXP-000979 - A2 gradient flow versus the R-192 production owner

## Scope

This T0, claim-nonbearing crosswalk audits a possible QFT/production-dynamics
candidate rather than asserting a new theorem tier.  The live A2 authority
provides, conditionally on `A2-H3-CANONICAL-PRODUCTION-FUNCTIONAL`, a
deterministic real-
`L2` gradient flow for the hash-pinned P1 functional on the fixed three-torus.
It also provides the fixed-torus energy identity, smoothing and exponential
decay statements within that conditional scope.

R-192 asks for a different owner chain: a production heat/root incidence,
root-labelled filtration, conditional replicas, a raw-current spatial
intertwiner and a once-owned nonnegative production q-ledger.  The A2 status and
functional manifests contain none of these fields.  The R-192 owner audit
therefore still stops at `heat_root_incidence`, whose current status is
`mapped=false, structural-only`.

## Exact crosswalk

| Object | A2 authority | R-192 requirement | Verdict |
|---|---|---|---|
| Evolution law | deterministic real-L2 gradient flow | production heat/root process | baseline only |
| Functional | fixed P1, `eta_shell=0` | same A1 cylinder plus dynamic owner | conditional match only |
| Stochastic heat | not supplied | required | absent |
| Root incidence | not supplied | first owner slot | absent; first failure |
| Conditional replicas | not supplied | required | absent |
| Raw-current spatial map | not supplied | required | absent |
| Production q-ledger | not supplied | required | absent |
| Physical empty/continuum | explicitly excluded | outside R-192 finite owner | not implied |

The deterministic gradient flow is therefore a legitimate conditional PDE
baseline, but it cannot be relabelled as the stochastic A13 production owner.
This is not a refutation of A2.  It is a separation of two different dynamic
contracts.

## Adversarial checks and reproduction

The primary and independent lanes both recompute the four pinned source hashes,
check the A2 conditional gradient-flow and fixed-torus statements, verify the
absence of all five R-192 owner slots, and confirm the R-192 first-failure
slot.  The integrated lane runs both children and rejects five hostile
mutations: promoting the gradient flow to a production heat/root map, erasing
the first failure, inserting stochastic heat, promoting physical-empty, or
inventing a q-ledger field.

```text
E:\Dev\TECT.venv\Scripts\python.exe -B -X utf8 codes/foundations/a2_gradient_flow_r192_owner_crosswalk.py --no-store
E:\Dev\TECT.venv\Scripts\python.exe -B -X utf8 codes/foundations/a2_gradient_flow_r192_owner_crosswalk_independent.py --no-store
E:\Dev\TECT.venv\Scripts\python.exe -B -X utf8 codes/foundations/a2_gradient_flow_r192_owner_crosswalk_verify.py --no-store
```

The fresh lanes pass 12/12, 12/12 and 19/19.  The existing Lean-backed R-192
owner audit remains the authority for the first missing slot; this crosswalk
does not replace or weaken it.

## Boundary and next obligation

No A13 gate, Sector-A, Pre-A, physical-empty, OS/KMS, real-time,
thermodynamic or continuum conclusion follows.  The next load-bearing input is
still a separately hash-pinned production heat/root map with its filtration,
conditional replicas, raw-current spatial identity and q-ledger.  Until those
objects are supplied, retain the A2 gradient flow as a conditional deterministic
baseline and do not rerun R-192 as though the missing owner had been supplied.

No PDF is issued at this intermediate checkpoint.
