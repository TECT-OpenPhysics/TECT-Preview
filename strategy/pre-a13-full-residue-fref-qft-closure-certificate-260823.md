# Full side-16 residue closure of the finite F_ref QFT candidate

## Scope

This is an exploration-only, T0, claim-nonbearing cross-check. It connects
the selected finite Euclidean stochastic-quantization candidate to the exact
finite Fourier algebra without promoting that candidate to the A1 production
dynamics owner.

## Finite Fourier algebra

On the side-16 torus the Fourier labels form the finite group
\(G=(\mathbb Z/16\mathbb Z)^3\). For a pointwise local term the Fourier
transform is the cyclic convolution

\[
 (a*b)(k)=\sum_{u+v=k\;\mathrm{mod}\;16}a(u)b(v).
\]

Therefore the full finite coordinate space \(\mathbb C^{3|G|}\) (with the
real-pairing constraint) is closed under the quadratic and sextic polynomial
parts of the hash-pinned \(F_{\rm ref}\) gradient. The identity-mobility
finite generator and its Gibbs cancellation are the already checked R-197
comparison candidate; this certificate adds the exact residue closure only.

## Root witness and alias-aware closure

For the one-component witness \(\phi_0=z+z^2\), the non-aliased Laurent
calculation gives

\[
 \phi_1=(\bar\phi_0\phi_0)^2\phi_0=z^{-1}(1+z)^5,
 \qquad
 \phi_2=z^{-11}(1+z)^{25}.
\]

The first non-aliased interval is \([-1,4]\), and the second is \([-11,14]\).
After reduction modulo 16, the first cyclic support is
\(\{15,0,1,2,3,4\}\), while the second cyclic support is every residue in
\(\mathbb Z/16\mathbb Z\). All coefficients are positive, so this is a
support statement, not cancellation hidden by zero coefficients.

Consequently the full-residue projection is a valid finite nonlinear
comparison space, but no proper coordinate projection containing the registered
two-root seed remains invariant under this nonlinear operation. A full-space
projection also erases the distinction between the \(k\) and \(2k\) root
labels; it is not the missing R-192 heat-root incidence map.

## QFT boundary

The result is a finite Euclidean Galerkin/stochastic-quantization interface.
It supplies neither a canonical mobility beyond the declared identity choice,
nor conditional replicas, a raw-current spatial intertwiner, or a once-owned
nonnegative \(q_k\) ledger. It gives no interacting measure, OS/KMS or
real-time reconstruction, thermodynamic/continuum limit, physical-empty
ordering, A13/T-050, Sector-A, or Pre-A closure.

The eight hostile mutations in the manifest are load-bearing. In particular,
cyclic aliasing must not be replaced by an infinite-line statement, and finite
Gibbs invariance must not be used as a substitute for the production
heat-root owner.
