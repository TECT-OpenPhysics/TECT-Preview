# Current A1 Class-II owner revalidation

## Scope

This is a T0, claim-nonbearing reader revalidation.  It does not rewrite the
historical R-172 package.  The unchanged primary and independent lanes are
run against the current tree, while current aggregate counts are derived from
their live authorities rather than copied from the historical manifest.

## Current evidence

The current integrated reader passes `18/18`.  Its primary and independent
children agree on the exact nonzero difference between the declared
`cKK*beta_X^2` Class-II numerator and the historical residual
`cJK*alpha_X*beta_X` numerator.  The positive mass denominator is retained.

The current R-200 mobility revalidation passes `27/27`, showing that the same
static inverse-covariance data still admit distinct positive heat rates.  The
unchanged R-192 owner-order audit passes its internal `58/58` checks but stops
at `heat_root_incidence` with `FAIL_FIRST_MISSING_PRODUCTION_MAP`.

The historical R-193 wrapper is not used as current evidence: it rejects the
current Lean registry hash before its mathematics runs.  This is a stale-reader
defect, not a mathematical refutation; R-200 is the current-authority repair
for that interface witness.

## Consequence and boundary

The A1 source has not selected between `F_decl` and the proposed `F_ref`, and
the static data do not select a canonical mobility or root-labelled heat map.
No QFT dynamics candidate may therefore be promoted to the TECT production
owner.  The R-192 filtration, conditional-replica, raw-current-intertwiner and
one-use `q`-ledger slots remain open.  No A13, Sector-A, Pre-A, OS/KMS,
continuum, thermodynamic, physical-empty or mass-gap conclusion follows.
