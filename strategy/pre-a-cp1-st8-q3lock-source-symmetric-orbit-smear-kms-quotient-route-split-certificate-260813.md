# R-167 v3.8 certificate: source-symmetric orbit-smear KMS and quotient route split

Date: 2026-08-13  
Task: T-054  
Exploration: EXP-000842  
Claim-bearing: false (T0 route work)  
PDF: none issued

## 1. Exact scoped result

This certificate closes only
`PA-CP1-ST8-Q3LOCK-SOURCE-SYMMETRIC-L1-ORBIT-SMEAR-CARRIER-AND-SELECTED-TANGENT-KMS-PAIR`.

Fix the microscopic Q3 regulator `N`, a physical inverse temperature
`beta>0` in the registered corridor, and a symmetric compact source interval
`I=[-h_0,h_0]`. The exact finite periodic source Hamiltonians define one
source-family categorical `C*`-system. The selected parity-paired Gibbs
tangent nets pull back to this one system, and every joint weak-star cluster
pair consists of two distinct physical-inverse-temperature-`beta` Gibbs/KMS
states, equivalently mathematical `(beta hbar)`-KMS states, for its common
action.

The physical KMS strip has width `beta hbar`, not `beta`. This is a
categorical orbit-smear theorem. It is not a spatial thermodynamic dynamics,
a zero-source quotient theorem, or a ground-state theorem.

## 2. The source-family orbit-smear carrier

Let `Xi_Q` be the countable set of finite-support rational configuration
labels on the coarse lattice. For every finite periodic `Lambda`, periodize
`xi` and write

```text
W_(Lambda,xi)=exp(i xi.q).
```

For `h in I`, let

```text
alpha_t^(Lambda,h)(A)
 = exp[it H_(Lambda,N)(h)/hbar] A exp[-it H_(Lambda,N)(h)/hbar].
```

For `f in L1(R)`, introduce a symbol `A_(xi,f)`, linear in `f`, and define
its finite representation by the weak-operator integral

```text
pi_(Lambda,h)(A_(xi,f))
 = integral_R f(t) alpha_t^(Lambda,h)(W_(Lambda,xi)) dt.       (2.1)
```

It satisfies

```text
||pi_(Lambda,h)(A_(xi,f))|| <= ||f||_1,
A_(xi,f)^* = A_(-xi,bar f).                                 (2.2)
```

Start with the unital formal star algebra generated by these symbols, impose
the displayed linear and star relations, and put

```text
||a||_(N,I)=sup_(finite periodic Lambda, h in I)
             ||pi_(Lambda,h)(a)||.                           (2.3)
```

Every formal polynomial has finite seminorm by (2.2). Quotient the common
null ideal and complete. The result is a unital separable `C*`-algebra
`A_(N,I)`. The product of all declared finite representations is faithful by
construction; no individual representation is asserted faithful.

For

```text
(tau_s f)(t)=f(t-s),
theta_s(A_(xi,f))=A_(xi,tau_s f),                             (2.4)
```

change of variables in (2.1) gives exact equivariance,

```text
pi_(Lambda,h)(theta_s a)
 =alpha_s^(Lambda,h)(pi_(Lambda,h)(a)).                       (2.5)
```

Thus `theta_s` is isometric. Moreover,

```text
||theta_s(A_(xi,f))-A_(xi,f)||_(N,I)
 <=||tau_s f-f||_1 ->0.                                      (2.6)
```

Polynomial product estimates and density extend this to every element.
Hence `theta` is a point-norm `C0` automorphism group independent of the
source value, tangent sign, state, and beta. This independence is only within
the fixed microscopic `N` and declared interval `I`.

## 3. Parity on the same carrier

Exact Q3 parity `P` obeys

```text
P H_(Lambda,N)(h) P = H_(Lambda,N)(-h),
P W_(Lambda,xi) P = W_(Lambda,-xi).                         (3.1)
```

Because `I=-I`, the rule

```text
gamma(A_(xi,f))=A_(-xi,f)                                   (3.2)
```

preserves the supremum norm, descends through the null ideal, and defines an
involutive automorphism of `A_(N,I)`. Equations (2.5) and (3.1) show that
`gamma theta_s=theta_s gamma`.

The symmetric source interval is load bearing. A one-sided family would not
by itself make (3.2) isometric in the declared supremum norm.

## 4. The selected tangent KMS pair

Let `h_n>0`, `h_n->0`, and let `Lambda_n` be the already registered selected
finite-volume tangent sequence at fixed `(N,beta)`. Pull back the exact Gibbs
states through the two representations:

```text
varphi_n^+ = rho_(Lambda_n,+h_n,beta) o pi_(Lambda_n,+h_n),
varphi_n^- = rho_(Lambda_n,-h_n,beta) o pi_(Lambda_n,-h_n).    (4.1)
```

Each is a state on the same unital algebra. Finite Gibbs cyclicity gives the
KMS boundary relation on the physical strip

```text
0 <= Im z <= beta hbar                                     (4.2)
```

for `theta`, because physical real time was normalized by `H/hbar` in
Section 2. Thus both states in (4.1) are physical-`beta` Gibbs/KMS pullbacks,
equivalently mathematical `(beta hbar)`-KMS states, for one common action. The
product of two weak-star compact state spaces supplies a joint cluster
subnet. For a point-norm continuous `C*`-dynamics, the KMS condition at the
fixed mathematical parameter `beta hbar` is weak-star closed, so the limits
`varphi_+` and `varphi_-` have the same property for `theta`. Exact parity
pairing gives

```text
varphi_n^- = varphi_n^+ o gamma,
varphi_-   = varphi_+ o gamma.                                (4.3)
```

This proves a common categorical action and a selected common-action KMS
pair. It does not identify a spatial all-exhaustion thermodynamic action, nor
does it prove that either limit factors through the zero-source carrier.

## 5. One fixed sine-smear separates the pair

Write `rho_n^+=rho_(Lambda_n,+h_n,beta)` and
`rho_n^-=rho_(Lambda_n,-h_n,beta)` for the concrete finite Gibbs states, and
retain `varphi_n^plus/minus=rho_n^plus/minus o
pi_(Lambda_n,plus/minus h_n)` for their states on the categorical carrier.
Along the selected plus tangent net, the registered corridor provides, after
discarding a finite prefix,

```text
rho_n^+(Q_0)>=m_0>0,
sup_n rho_n^+(|Q_0|^4)<=C_4<infinity.                          (5.1)
```

One safe corridor choice is `m_0=sqrt(delta_beta)/2`. Put

```text
X=sqrt(8)Q_0=sum_(e=1)^8 q_e,
M_3=(64 C_4)^(3/4).                                          (5.2)
```

Lyapunov's moment inequality gives

```text
rho_n^+(|X|^3)<=M_3.                                         (5.3)
```

Choose a fixed positive rational `r` such that

```text
r^2 M_3<=3 sqrt(8)m_0,                                      (5.4)
```

and use the rational one-site label `xi=r(1,...,1)`. Since
`|sin z-z|<=|z|^3/6`, (5.1)--(5.4) give

```text
rho_n^+(sin(rX))
 >=r sqrt(8)m_0-r^3 M_3/6
 >=r sqrt(8)m_0/2=:d>0.                                    (5.5)
```

Parity gives the corresponding minus expectation at most `-d`.

Choose any real nonnegative `f in L1(R)` with
`integral f=||f||_1=1`, and define the single abstract observable

```text
b=[A_(xi,f)-A_(-xi,f)]/(2i).                                (5.6)
```

Then `b=b*` and `||b||<=1`. Exact Gibbs invariance and the pullback
definition, not an approximate small-time argument, give the
concrete-to-categorical bridge

```text
varphi_n^+(b)=rho_n^+(sin(rX))>=d,
varphi_n^-(b)=rho_n^-(sin(rX))<=-d.                         (5.7)
```

The inequalities pass to the selected joint cluster pair. Therefore

```text
||varphi_+-varphi_-||>=2d.                                  (5.8)
```

The witness is fixed before taking the subnet. Moving witnesses would not
prove distinct weak-star limits.

## 6. The canonical zero-source quotient and its exact criterion

Repeat Section 2 using only `h=0`, obtaining `A_(N,{0})`. Since `0 in I`,

```text
||a||_(N,{0})<=||a||_(N,I).                                 (6.1)
```

The identity on formal generators therefore extends to a canonical
surjective equivariant star homomorphism

```text
q_0:A_(N,I)->A_(N,{0}).                                     (6.2)
```

Surjectivity follows because its image contains the dense generator algebra
and a `C*`-homomorphism has closed range.

A state `omega` on `A_(N,I)` factors as `bar omega o q_0` if and only if

```text
omega(k* k)=0 for every k in ker q_0.                        (6.3)
```

Necessity is immediate. Conversely, Cauchy--Schwarz makes (6.3) imply
`omega(ak)=omega(ka)=0` for every `a` and kernel element `k`; hence `omega`
is constant on quotient classes and induces a positive normalized functional
on `A_(N,{0})`. Equivalently, factorization holds exactly when `omega`
vanishes on `ker q_0`.

The scalar fact `h_n->0` does not imply (6.3), because the source-dependent
orbit can retain a finite phase on time-smear elements.

## 7. Exact M2 obstruction to automatic factorization

Let `n` range over the positive integers, put `I_2=[-1,1]`, set `hbar=1`,
and, on `C^2`, take

```text
D=diag(0,1),       H_n(h)=n h D,
h_n=1/n,           B=sigma_x,
f=1_[0,1]-1_[1,2].                                      (7.1)
```

Use the analogous unital `M2` source-family orbit-smear carrier `A_M2` with
declared seed `B=sigma_x` and supremum over every pair `(n,h)` with positive
integer `n` and `h in I_2`; this seed is not a Q3 rational-character label.
The zero-source carrier `A_(M2,0)` uses the subfamily of all `(n,0)`, and
restriction of the supremum defines the canonical quotient

```text
q_0^M2:A_M2->A_(M2,0).                                      (7.2)
```

Let `a=A_(B,f)` denote the common orbit-smear symbol. At zero source,
the orbit of `B` is constant and `integral f=0`, so

```text
q_0^M2(a)=0.                                             (7.3)
```

At the vanishing source `h_n`, the product `n h_n` is exactly one. Direct
integration gives

```text
pi_(n,h_n)(a)=c E_01+bar(c) E_10,
c=integral f(t)exp(-it)dt=(1-exp(-i))^2/i.               (7.4)
```

Consequently

```text
pi_(n,h_n)(a* a)=|c|^2 I_2
                =16 sin(1/2)^4 I_2.                     (7.5)
```

The right side is strictly positive and independent of `n`. In particular,
every normalized state at `(n,h_n)` has expectation
`16 sin(1/2)^4` on `a*a`, although `a in ker q_0^M2`. Pulling these states
back to the one unital `A_M2`, weak-star compactness gives cluster states, and
every such cluster violates the M2 analogue of (6.3). This proves the exact
route-level negative

`NG-2026-08-13-PRE-A-ST8-Q3LOCK-VANISHING-SOURCE-AUTOMATIC-ZERO-SOURCE-QUOTIENT-FACTORIZATION`.

This `M2` construction is not a Q3LOCK counterexample. It refutes only a
general logical shortcut. The selected Q3 tangent states may factor through
`q_0`, but doing so requires a model-specific kernel estimate not supplied by
source convergence alone.

## 8. Devil's-advocate audit

1. **Objection: the carrier depends on the source, so there is no common
   action.** **DISMISSED in the declared categorical scope.** One supremum
   completion includes every `h in I`, and kernel translation defines a
   single action before choosing a state. This does not make it a spatial
   thermodynamic action.

2. **Objection: the KMS strip has the wrong units.** **DISMISSED.** With
   `alpha_t=Ad exp(itH/hbar)`, the imaginary-time displacement is
   `i beta hbar`; every statement uses that physical strip.

3. **Objection: the two limits could coincide because the separating
   observable changes with volume.** **DISMISSED.** Rational `r` and the
   probability kernel `f` are chosen once. The same `b in A_(N,I)` gives the
   uniform bounds `+d` and `-d`.

4. **Objection: `h_n->0` makes every source-family element converge to its
   zero-source image.** **UPHELD as an overclaim.** The exact `M2` fixture has
   `q_0^M2(a)=0` but a fixed nonzero value of every state on `a*a` along
   `h_n=1/n`. The kernel criterion must be proved separately.

5. **Objection: common categorical physical-`beta` KMS states are already algebraic ground
   states or a broken-sector GNS gap.** **UPHELD as an overclaim.** Beta is
   fixed; no beta-infinity passage, ground-form inequality, GNS simplicity or
   positive spectral-gap estimate appears here.

6. **Objection: the construction evades the earlier categorical no-go
   records.** **DISMISSED.** It retains the posthoc-direct-sum,
   cross-beta-gluing and categorical-envelope boundaries. No spatial
   all-shape Cauchy or unique thermodynamic phase quotient is inferred.

## 9. Scope and next gate

EXP-000842 establishes R-167 v3.8 as additive T0, claim-nonbearing route
work. It supplies one exact source-symmetric categorical carrier, one
selected distinct physical-`beta` KMS pair, with mathematical KMS parameter
`beta hbar`, on its common action, the canonical
zero-source quotient criterion, and one exact failure of automatic
factorization.

It proves no exact Q3 zero-source thermodynamic or spatial common alpha, no
all-exhaustion Cauchy theorem, no zero-source quotient factorization for the
selected pair, no beta-infinity ground selection, no algebraic ground-state
identity, purity or complete phase classification, no broken-sector GNS or
physical mass gap, no continuum or regulator removal, no physical vacuum or
empty-space comparison, Round-1, C6, CP1, physical Sector A or Pre-A. All
five active parent gates and both historical gates
`PA-CP1-ST8-Q3LOCK-BETA-INFINITY-GROUND-STATE-SELECTION` and
`PA-CP1-ST8-Q3LOCK-DLR-TO-COMMON-ALPHA-KMS-IDENTIFICATION` remain OPEN. No
v3.8 PDF is issued.

The next independent step is to prove the zero-source kernel condition for
the selected Q3 pair or to identify algebraic ground states through a common
generator-form residual theorem. Neither is smuggled into this result.
