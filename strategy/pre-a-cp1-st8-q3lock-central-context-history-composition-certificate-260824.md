# EXP-001048 — Conditional central-context Q3 history composition

## Status

This is a T0, claim-nonbearing checkpoint. It records an exact finite common-core composition theorem under an explicit four-context hypothesis and reuses the registered EXP-001045 Q3 one-step source rate. It does not prove the hypothesis for the Q3 operators.

The proposed central context is

\[
 M_{a,b}(D)=\lVert A^a D A^{-b}\rVert,\qquad a,b\in\{0,s\},\quad s=3/4.
\]

If every source difference has all four contexts bounded by the same \(B\), then on the declared common core

\[
 A^aD_1\cdots D_nA^{-b}
 = (A^aD_1A^{-s})(A^sD_2A^{-s})\cdots(A^sD_nA^{-b}).
\]

Submultiplicativity therefore gives the conditional word bound \(B^n\). The primary and independent lanes test this identity and bound on the exact finite fixture \(A=\operatorname{diag}(1,16,256)\), with \(A^{3/4}=\operatorname{diag}(1,8,64)\), using factors \(B,B/2,B/3,B/4\).

The actual Q3 rate \(B\) is recomputed from the EXP-001045 source-polynomial fixture rather than pasted. With the separate factorial first-passage hypothesis, orientations two, degree six, spatial base two and \(t=1/1000\), the scalar EGF exponent is

\[
 \eta=2\cdot6\cdot2\cdot B\cdot10^{-3}.
\]

This is only a conditional numerical insertion. The package does not supply the factorial expansion, the four central contexts, boundary decay, exhaustion Cauchy convergence, common \(\alpha\), OS/KMS reconstruction, a GNS gap, continuum removal, C6, Sector A, Pre-A, or the TECT production owner.

## Devil's-advocate review

1. The central-context table is an assumption, not a consequence of the one-sided EXP-001045 graph lift. This objection is upheld.
2. The insertion cancels \(A^{-s}A^s\) only on the declared common core; no domain closure is inferred. Upheld.
3. The finite matrices do not identify the actual unbounded Q3 source differences. Upheld.
4. The exponential envelope assumes a nonnegative factorial Duhamel/first-passage expansion that remains an open gate. Upheld.
5. The explicit spatial base prevents the conditional scalar estimate from being misread as moving-site uniformity or an exhaustion theorem. Upheld.
6. Lean R230 proves finite algebra and rational arithmetic only. Upheld.
7. No QFT or TECT production conclusion follows. Upheld.

## Next gate

Attack the actual four-context estimate for Q3 source differences, retaining both orientations and spatial weights. If it cannot be made volume- and source-uniform, record the exact obstruction before revisiting the factorial recurrence.
