# Q3LOCK EXP-000782 upstream scope-firewall audit

**Status:** T0 publication-scope audit; no claim-card promotion
**Date:** 2026-09-05
**Owner task:** T-054
**Authority:** EXP-000780 -> EXP-000781 -> EXP-000782
**Audited source:** `strategy/pre-a-cp1-st8-q3lock-positive-lambda-fkg-infrared-cusp-phase-route-split-certificate-260804.md`
**Audited source SHA-256:** `b6487a9381bef20cdf1a9abc4dfdec9aa40f69b0b73697595c263ffe574a4d89`
**PDF:** deferred until mathematical content, independent review and claim/result scope are frozen

## 1. Purpose and strict boundary

EXP-000782 is the sole upstream candidate proof certificate in the Q3LOCK
authority chain.  Its historical text contains theorem-like result-first
language, while the current R-497 registration correctly classifies the
assembled material as `T0 / INTERNAL_REVIEW_ONLY` and claim-nonbearing.  This
note is a scope firewall: it records the discrepancy without rewriting the
historical certificate and fixes the wording that may be used in a future
manuscript before the open audits close.

This is not a mathematical certification, a claim card, an external referee
report, or a publication decision.  It does not promote EXP-000782 or R-497,
and it does not create a LaTeX source or PDF.

## 2. Byte-pinned observations

The frozen upstream certificate contains the following substantive statements.

1. Its opening result-first paragraph says that the certificate “closes a
   genuine positive-lambda phase theorem” and lists a strict cusp and two
   distinct parity-related DLR states under the displayed threshold.
2. Section 8 says that the displayed pressure and tangent-state equations
   “prove” the positive slope and that the states “are distinct”.
3. Section 10 labels the finite-time FKG, infrared, cusp and DLR items
   “Closed here”.
4. The same certificate is labelled `Tier: T0, claim-nonbearing` at its
   header and preserves nonclaims for KMS, ground state, continuum and the
   cosmological/TECT routes.

The source bytes and hash are retained as historical EXP-000782 evidence.  No
sentence above is silently edited in place.  The current independent result
manifest instead records every load-bearing passage as assembled, conditional,
or pending independent audit, and explicitly keeps the claim and publication
statuses at T0 and research-only.

## 3. Scope decision for the independent result

The theorem-like sentences in the upstream certificate cannot be copied into
the independent paper as unconditional completed results at the present stage.
The permitted current description is:

> EXP-000782 supplies a conditional proof-spine candidate for the fixed-
> spacing positive-lambda Q3LOCK model.  The finite pressure, tempered DLR,
> continuous-loop FKG, reflection-positive infrared, operator, Griffiths and
> source-tangent passages have been assembled in separate T0 audit notes, but
> their independent hypothesis checks, limit compositions, claim-card
> decision, content freeze and external mathematical review remain open.

The eventual theorem statement may be promoted only after every listed bridge
has an independent disposition of `PASS` or an explicitly repaired successor,
the source hypotheses are matched line by line, and the claim/result registry
is updated in the same atomic change set.  Until then, “proves”, “closed”,
“phase transition”, “strict cusp”, and “two states exist” are not acceptable
unqualified manuscript assertions; they must be conditional on the stated
bridge lemmas.

## 4. Why the firewall is necessary

The certificate's algebra and the current audit notes are not the same as a
completed external proof.  In particular, the following interfaces remain
load-bearing:

- finite time-grid association to the continuous-loop FKG inequality;
- finite-dimensional FSS domination to the continuous-loop Duhamel bound;
- common form-domain and unbounded-operator trace differentiation;
- source-window uniform integrability and source-to-zero DLR specification
  continuity;
- the KKK endpoint-interval pressure conversion and the exact thermodynamic
  limit order; and
- a bounded independent claim-card/result-lineage decision followed by
  clean replay and external mathematical review.

An executable primary/independent/integrated PASS only checks the registered
conditional packet and its hashes.  It does not remove these analytic or
source-hypothesis obligations.  This distinction is required by the
verification-first governance and prevents the historical result-first prose
from becoming an accidental overclaim.

## 5. Adversarial checks

| Objection | Disposition | Consequence |
|---|---|---|
| The upstream phrase “closes a theorem” is itself the current claim tier. | **UPHELD AS A SCOPE CONFLICT** | Treat it as historical certificate prose, not as the present R-497 status. |
| Correcting the wording requires rewriting EXP-000782. | **DISMISSED** | Preserve the byte-pinned source and use this overlay note plus the manifest as the current scope authority. |
| Three verifier PASS results certify every analytic bridge. | **UPHELD AS FALSE** | They certify the conditional reproducibility packet only; independent proof and referee gates remain. |
| Calling the result conditional weakens the intended final theorem permanently. | **DISMISSED** | It is a temporary publication firewall; promotion is allowed after the explicit gates close. |
| PDF generation can proceed because the source certificate already says “Closed”. | **UPHELD AS FALSE** | PDF and manuscript creation remain blocked by content freeze and independent review. |

## 6. Disposition and next gate

**Advanced at T0:** the historical/up-to-date scope discrepancy is explicitly
identified, the upstream hash is pinned, and a permitted conditional wording
is supplied for later manuscript use.  No scientific result, bound, source
normalization or authority-chain input is changed.

**Still open:** independent P-06/P-09, operator/form-domain, source-tangent,
KKK and theorem-hypothesis audits; bounded claim-card decision; complete
source-hash freeze after content review; clean-snapshot replay; release check
on that frozen tree; and external mathematical referee review.

## 7. Explicit nonclaims and PDF gate

This audit does not assert a strict cusp, positive-lambda phase transition,
DLR multiplicity, extremality, purity, clustering, a common real-time
dynamics, a KMS state, a ground-state phase or gap, a continuum limit,
physical vacuum, cosmological interpretation, C6, CP1, Sector A or Pre-A
closure.

No claim card, P2 manuscript, submission, upload, tag, release or PDF is
created.  Final LaTeX compilation, PDF rendering, page-by-page visual review
and artifact hash capture are permitted only after the proof text and scope
have passed independent review and have been content-frozen.
