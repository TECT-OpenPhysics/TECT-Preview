# Pre-A Round-1 frozen inventory and categorical triage

**Result:** `PRE-A-ROUND1-T053-FROZEN-INVENTORY-AND-NO-CURRENT-ADMITTED-MICROSCOPIC-SURVIVOR`  
**Task:** T-054  
**Date:** 2026-08-10  
**Tier:** T0, claim-nonbearing

## 1. Result first

The bounded T-053 evidence-role inventory, four candidate records, common
categorical questions, reference rules, and robustness boundaries are now
frozen in a single versioned contract.  The current Round-1 set contains no
admitted microscopic survivor:

- M1 is rejected only as the pinned unconstrained ordered-vacuum functional
  and is not admitted to the physical T-053 test because no conservative
  real-time law or microscopic observable map is declared.
- M2 retains an exact isotropic tree cone in its separately inserted inertial
  lane, but is not admitted because the physical mode map, compact/gauge map,
  critical data and robustness contract are absent.
- bare M5 retains its static shell result but fails the joint three-dimensional
  cone in the exact rank-one shell scope and has no admitted compact gauge law.
- M0 is the effective null baseline, not a microscopic contestant.

This is an honest non-selection, not a universal falsification.  The parent
gate `PA-ROUND1-EVIDENCE-ROLE-AND-MINIMUM-MANIFEST-FREEZE` remains **open**.
The visible helium target was public before the new stiffness map was written,
so its mismatch receives no validation credit.

## 2. Evidence-role firewall

Every item in the inherited boundary register is assigned once.  Propagation
observations constrain speed and dispersion only.  In particular,
`PA-EV-T053-002` does not determine a microscopic zero-mode or polarization
count.  Direct vacuum winding, microscopic mode-count, cosmological-cooling,
particle-spectrum, gravitational-response and physical-empty evidence remain
explicitly missing or deferred.

The superconducting flux item is a mechanism calibration, not vacuum
evidence.  The helium exponent is a visible retrospective diagnostic target,
not a blind holdout.  These distinctions prevent one physical example from
being counted twice under different names.

## 3. Common categorical contract

The only allowed cell outcomes are `PASS`, `FAIL`, `NOT_TESTED`,
`NOT_ADMITTED`, and `INCOMPARABLE`.

- `FAIL` refutes only the declared cell mechanism and parameter scope.
- `NOT_ADMITTED` means a required law, map, state, or contract is absent.
- `NOT_TESTED` means the declared object was not tested.
- `INCOMPARABLE` means no common comparison contract exists.

Here D00 is deliberately a syntactic admission row: `PASS` means that all
nine canonical field labels are present and that every absence or partial
status is explicit.  It does not certify semantic completeness or a complete
per-parameter common-input ledger.  Both stronger requirements remain open at
the parent gate.

An eligible microscopic candidate survives only if all ten hard rows D00--D09
are `PASS`.  The primary and independent verifiers compute this rule from the
matrix and both return the empty set; they do not trust the manifest's empty
list as an oracle.

Candidate-internal energies may be signed only under the same law, reference,
volume, boundary, regulator, normalization and additive scalar.  Raw energies
of different Hamiltonians are not ranked.

## 4. Exact quadratic route checks

For M2,

\[
 K(k)=r+c\sum_{i=1}^3(q^2-k_i^2)^2.
\]

At a critical node `k_star in {+q,-q}^3`, exact expansion gives

\[
 D_k^2K(k_\star)=8cq^2I_3,
 \qquad
 \omega^2={4cq^2\over\chi}|p|^2+O(|p|^3).           \tag{4.1}
\]

Thus the inserted inertial lane has an isotropic tree speed
`4cq^2/chi`.  This candidate-internal statement is not a D04 physical pass:
the all-mode observable map and comparison to the propagation register are
absent.

M1's registered law is first-order L2 gradient flow with no canonical
momentum.  Bare M5's continuous isotropic-shell Hessian is rank one.  Adding
inertia, an isolated-node anisotropy, compact target, gauge quotient, or a new
observable map creates a new candidate version and must be frozen again.

## 5. Retrospective stiffness diagnostic

The newly declared analytic mean-field map assumes

\[
 f(A,t)={a t\over2}A^2+{u_{\rm eff}\over4}A^4,
 \qquad \rho_s=Z(t)A^2,                              \tag{5.1}
\]

with positive analytic `a`, `u_eff`, and `Z(0)`.  Minimization gives
`A^2 proportional to |t|`, hence the exact tree exponent is one.  The visible
target interval is `[0.671,0.673]`, so this formula conflicts with it.

However, the target predates this formula.  Observable-map selection leakage
cannot be excluded.  Therefore D07 is not passed or failed for validation
credit; it remains `NOT_TESTED` for M2, and the numerical mismatch is recorded
only as a retrospective mechanism diagnostic.

## 6. Remaining exact gate

To close the parent Round-1 gate, the project must first freeze either a new
future/blind target or a genuinely new relation whose result is not yet known,
together with the microscopic observable map and complete per-parameter input
ledger.  This freeze must exist in an immutable earlier commit before scoring.

Candidate generation may proceed in parallel.  The minimum useful next
versions need genuine conservative microscopic dynamics, a compact/gauge
configuration and physical observable map; a structurally distinct alternative
should isolate protected nodes rather than reuse a continuous isotropic shell.

## 7. Adversarial review and boundary

1. **Was the visible exponent preregistered?** No.  Earlier drafts incorrectly
   implied this; the final package removes gate credit.
2. **Does no current survivor falsify every possible TECT law?** No.  It says
   only that M1/M2/M5 in their frozen versions fail full admission.
3. **Does the M2 cone pass the physical speed test?** No.  It is an exact
   internal result for an inserted inertial lane; D04 remains not admitted.
4. **Are the nine manifest fields semantically complete?** No.  Their labels
   and explicit absences are frozen, while the per-parameter input ledger and
   observable maps remain incomplete.

No physical functional, vacuum, common dynamics, continuum, empty-space sign,
C6, CP1, A13, Sector-A or Pre-A completion follows.
