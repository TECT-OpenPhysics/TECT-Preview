# EXP-001027 certificate: critical graph seminorm onsite-stability obstruction

## Finding

The candidate seminorm is

\[
 L_K(A)=\sum_\nu\bigl(\|[q_\nu,A]K^{-1/2}\|+\|K^{-1/2}[q_\nu,A]\|
 +\|[p_\nu,A]K^{-1/2}\|+\|K^{-1/2}[p_\nu,A]\|\bigr),
\]

with `K=h-inf(spec(h))+1 >= 1`.  For the Weyl observable
`W_a=exp(-i a p_0/hbar)` and `a>0`, the exact commutators are
`[q_0,W_a]=a W_a` and `[p_nu,W_a]=0`.  Weyl unitarity and `K>=1` therefore
give the initial upper envelope `L_K(W_a)<=2a`.

The critical-half authority supplies the analytic lower bound, with
`t_a=tau/a^2`,

\[
 \|[p_0,\alpha_{t_a}(W_a)]K^{-1/2}\|\ge G\tau a-B_\tau,
 \qquad G=g+3\lambda>0.
\]

If the proposed onsite estimate
`L_K(alpha_t(A)) <= (1+C|t|) L_K(A)` held with frequency-independent `C`,
then the lower-minus-upper difference would be

\[
 D(a)=(G\tau-2)a-B_\tau-2C\tau/a.
\]

The manifest fixture derives `G=51/35`, `tau=2`, `B_tau=3/2`, `C=1`, so
`D(a)=(32/35)a-3/2-4/a`.  It is `507/70>0` at `a=10` and tends to positive
infinity.  This is an exact contradiction to the proposed bound for this
declared seminorm, conditional on the imported analytic lower bound.

## Decision and boundary

`EXP-001027` is an advanced T0, claim-nonbearing route obstruction.  It
removes this particular critical `K^(-1/2)` graph seminorm from the candidate
tournament.  It does not reject all non-Leibniz analytic/Frechet or
state-weighted topologies, and it does not prove or disprove existence of Q3
dynamics.  It also supplies no common `alpha`, KMS state, ground/GNS gap,
continuum limit, heat-root incidence, A1/R-192 production map, Pre-A closure,
Sector-A closure or Clay result.

The primary SymPy lane and independent Fraction lane each pass 19/19; the
integrated lane passes 21/21; Lean R211 passes its scalar checks.  No PDF is
issued at this intermediate route checkpoint under the repository PDF economy.

## Adversarial review

- **Imported lower bound — UPHELD:** the analytic lower bound remains an
  explicit dependency on the critical-half authority; this package does not
  silently reprove it.
- **Fixture constants — UPHELD:** `B_tau`, `C` and `a=10` are test fixtures.
  The route rejection uses the derived positive slope gap, not a hidden
  universal oracle.
- **Topology scope — UPHELD:** one candidate seminorm is rejected; broader
  frequency-profiled, analytic/Frechet and state-weighted alternatives remain
  open.
- **Lean promotion — UPHELD:** R211 proves only rational arithmetic and the
  finite fixture inequality, not an unbounded-operator lower-bound theorem.
- **QFT-to-TECT promotion — UPHELD:** this is auxiliary QFT evidence.  The
  TECT production owner and `heat_root_incidence` ledger remain absent.

## Next gate

Test a frequency-profiled analytic/Frechet or state-weighted topology on a
declared common core, and separately continue the projected-Duhamel and
local-strict history routes.  No QFT evidence is promotable until the actual
onsite recurrence, spatial boundary decay, exhaustion Cauchy, common `alpha`
and KMS/ground-gap interfaces are explicit.
