# R-167 v4.1 certificate: categorical ground bandlimited GNS Poincare route split

Date: 2026-08-14  
Task: T-054  
Exploration: EXP-000845  
Claim-bearing: false (T0 route work)  
PDF: none issued

## 1. Exact scoped result

This certificate closes only
`PA-CP1-ST8-Q3LOCK-ZERO-SOURCE-CATEGORICAL-GROUND-BANDLIMITED-GNS-ENERGY-FORM-CORE-AND-PARITY-POINCARE-REDUCTION`.

R-167 v4.0 constructed two distinct parity-related algebraic ground states
`omega_+` and `omega_-` on one zero-source categorical C-star dynamical
system `(A_H^0,theta)`. It also constructed a compact-Arveson unital star
graph core `D_bl` for the generator `delta_H`. The present result identifies
the exact positive GNS energy form on that core and proves that its centered
vectors are automatically a form core for the implementing Hamiltonian on
the orthogonal complement of the cyclic ground vector.

For either sign `sigma`, the exact Poincare constant is therefore

```text
Delta_sigma^P
 =inf_(A in D_bl, Var_(omega_sigma)(A)>0)
   {-i hbar omega_sigma(A^* delta_H(A))
      /Var_(omega_sigma)(A)}.                              (1.1)
```

Positivity of (1.1) forces the implementing zero-energy kernel to be simple
and gives the corresponding GNS spectral gap. Parity implements a unitary
equivalence between the two sign systems after twisting observables by the
parity automorphism, so

```text
Delta_+^P=Delta_-^P.                                      (1.2)
```

This is a reduction, not a positive-gap theorem. No positive lower bound for
(1.1) is proved.

The same package closes a model-specific route boundary. The exact finite-
volume mesoscopic-source ground pairs of v4.0 have full excitation gaps
satisfying

```text
Delta_(L,sigma)^full
 <=[32 sqrt(B_a)/(r_w^2 rho_*)] h_L V ->0.                (1.3)
```

Thus their full finite-volume gaps cannot be the uniform positive Poincare
input required by the R-167 v3.0 transfer theorem. The trial proving (1.3)
switches the global parity branch and depends on `L`; it is not one fixed
element of `D_bl`. Equation (1.3) therefore does not refute a positive
phasewise GNS gap.

The registered new negative result is exactly
`NG-2026-08-14-PRE-A-ST8-Q3LOCK-MESOSCOPIC-SOURCE-FULL-FINITE-GAP-AUTOMATIC-UNIFORM-POINCARE-TRANSFER`.
The broader abstract boundary
`NG-2026-08-11-PRE-A-ST8-Q3LOCK-ORDERED-GROUND-DOUBLETS-AUTOMATIC-GNS-GAP`
is reused, not duplicated.

## 2. Inherited v4.0 system and conventions

Use the R-167 v4.0 zero-source categorical system

```text
(A_H^0,theta),
theta_(t+s)=theta_t theta_s,
||theta_t(A)-A||_H ->0 as t->0,                           (2.1)
```

with closed generator

```text
delta_H(A)=d/dt theta_t(A)|_(t=0).                        (2.2)
```

The finite exact-Q3 representations obey

```text
pi_L^0(theta_t A)=alpha_t^(L,0)(pi_L^0(A)),
alpha_t^(L,0)(B)=exp(i t H_L(0)/hbar)
                  B exp(-i t H_L(0)/hbar).                (2.3)
```

Let `D_bl` be the unital star-algebra generated by

```text
theta_g(B)=int_R g(t)theta_t(B)dt,                         (2.4)
```

where `B in A_H^0`, `g` is Schwartz, and `hat g` has compact
support. Compact Arveson bandwidths add under products and change sign under
star. R-167 v4.0 proves that `D_bl` is a unital star graph core for `delta_H`.

Along one fixed joint v4.0 cluster sequence,

```text
omega_L^sigma -> omega_sigma,
omega_-=omega_+ o gamma,
gamma theta_t=theta_t gamma,
gamma(D_bl)=D_bl.                                         (2.5)
```

Each `omega_sigma` is a theta-invariant algebraic ground state. The
finite-volume vectors are the exact normalized grounds `phi_L^sigma` of

```text
H_L(sigma h_L)=H_L(0)-sigma h_L S_L,
V=L^3,
h_L V^2->infinity,
h_L V->0.                                                 (2.6)
```

Put

```text
s_L^sigma=<phi_L^sigma,S_L phi_L^sigma>,
|s_L^sigma|<=C_S V,
C_S:=sqrt(B_a).                                           (2.7)
```

The exact v4.0 source form and residual are

```text
cal E_L^sigma(A)
 :=q_(H_L(sigma h_L)-E_L(h_L))
       [pi_L^0(A)phi_L^sigma]
 =-i hbar omega_L^sigma(A^*delta_H(A))-R_L^sigma(A),       (2.8)

R_L^sigma(A)->0,             A in D_bl.                   (2.9)
```

Equation (2.8) is a quadratic-form identity. The source commutator inside
`R_L^sigma` is a represented vector/form pairing. No claim is made that the
unbounded source commutator is a bounded operator.

The fixed v4.0 contraction `b` obeys, eventually,

```text
||b||<=1,
omega_L^+(b)>=d/2,
omega_L^-(b)<=-d/2,
d=r_w sqrt(rho_*)/2>0.                                   (2.10)
```

Here `r_w` is one fixed positive rational chosen before cluster extraction.

## 3. Canonical GNS implementation and exact energy identity

Fix one sign and temporarily omit `sigma`. Let

```text
(pi,H_GNS,Omega)
```

be the GNS triple of `omega`. Invariance gives a strongly continuous unitary
group defined on the dense cyclic subspace by

```text
U(t)pi(A)Omega=pi(theta_t(A))Omega.                        (3.1)
```

The group is well-defined because

```text
||pi(theta_t(A))Omega||^2
 =omega(theta_t(A^*A))
 =omega(A^*A).                                            (3.2)
```

Surjectivity follows from `theta_-t`, so (3.1) extends to a unitary. The
algebraic ground condition is equivalent to positivity of its physical
generator. Use the convention

```text
U(t)=exp(i t H/hbar),
H>=0,
H Omega=0.                                                (3.3)
```

For `A in Dom(delta_H)`, the norm difference quotient in the C-star algebra
converges. Applying the contractive representation to it gives

```text
d/dt U(t)pi(A)Omega|_(t=0)=pi(delta_H(A))Omega.            (3.4)
```

Stone's theorem and (3.3) give the other expression for the same derivative,

```text
d/dt U(t)pi(A)Omega|_(t=0)
 =(i/hbar)H pi(A)Omega.                                   (3.5)
```

Hence every `A in Dom(delta_H)`, and in particular every `A in D_bl`, obeys

```text
H pi(A)Omega=-i hbar pi(delta_H(A))Omega.                 (3.6)
```

Pairing (3.6) with `pi(A)Omega` proves the exact identity

```text
-i hbar omega(A^*delta_H(A))
 =<pi(A)Omega,H pi(A)Omega>
 =||H^(1/2)pi(A)Omega||^2>=0.                             (3.7)
```

Since `H Omega=0`, putting

```text
xi_A:=pi(A)Omega-omega(A)Omega                            (3.8)
```

does not change the energy:

```text
||H^(1/2)xi_A||^2
 =-i hbar omega(A^*delta_H(A)).                           (3.9)
```

No faithfulness, purity, factoriality, disjointness or simple-kernel
hypothesis entered this proof.

## 4. Compact-Arveson centered vectors form a GNS form core

We prove the form-core property that R-167 v1.8 and v3.0 previously treated
as an explicit premise. Let

```text
C_D={xi_A:A in D_bl}.                                     (4.1)
```

By (3.6), `C_D` lies in `Dom(H)`, hence in `Dom(H^(1/2))`, and is orthogonal
to `Omega`.

Take arbitrary

```text
xi in Dom(H^(1/2)) intersect Omega^perp.                  (4.2)
```

Choose real even functions `chi_R in C_c^infinity(R)` such that

```text
0<=chi_R<=1,
chi_R(E)=1 for |E|<=R,
supp chi_R subset [-2R,2R].                               (4.3)
```

Spectral dominated convergence gives

```text
xi_R:=chi_R(H)xi ->xi                                     (4.4)
```

in the form norm

```text
||zeta||_(H,1/2)^2
 :=||zeta||^2+||H^(1/2)zeta||^2.                          (4.5)
```

Pin the Fourier convention

```text
hat g(nu)=int_R exp(+i nu t)g(t)dt.                       (4.6)
```

Define `g_R` by

```text
hat g_R(nu)=chi_R(hbar nu).                               (4.7)
```

Then `g_R` is Schwartz and `hat g_R` has compact support. Under the already
pinned convention `U(t)=exp(i t H/hbar)`, the implementing spectral theorem
gives without a sign ambiguity

```text
pi(theta_(g_R)(B))Omega
 =int g_R(t)U(t)pi(B)Omega dt
 =hat g_R(H/hbar)pi(B)Omega
 =chi_R(H)pi(B)Omega.                                    (4.8)
```

Because `Omega` is cyclic, choose `B_n in A_H^0` with

```text
pi(B_n)Omega ->xi.                                        (4.9)
```

At fixed `R`, equations (4.8)--(4.9) imply Hilbert convergence

```text
chi_R(H)pi(B_n)Omega ->xi_R.                              (4.10)
```

Every vector in (4.10) has spectral support in one compact interval. On that
interval `H^(1/2)` is bounded, so (4.10) upgrades to form-norm convergence.
Moreover `xi_R` is orthogonal to `Omega` because the spectral projection onto
`Omega` commutes with `H`. Therefore subtracting

```text
<Omega,chi_R(H)pi(B_n)Omega>Omega                         (4.11)
```

preserves the limit. By (4.8), the centered vector in (4.11) is precisely
`xi_A` for

```text
A=theta_(g_R)(B_n) in D_bl.                               (4.12)
```

First choose `R` large in (4.4), then `n` large in (4.10), and diagonalize.
This proves

```text
closure_form(C_D)
 =Dom(H^(1/2)) intersect Omega^perp.                      (4.13)
```

Importantly, (4.13) remains true if `H` has other zero vectors orthogonal to
`Omega`. Kernel simplicity will be a consequence of a positive Poincare
constant, not an assumption smuggled into the core proof.

## 5. Exact Rayleigh equality and the simple-kernel consequence

The centered-vector norm is exactly

```text
||xi_A||^2
 =omega(A^*A)-|omega(A)|^2
 =:Var_omega(A).                                         (5.1)
```

Define

```text
Delta_omega^P
 :=inf sigma(H restricted to Omega^perp).                 (5.2)
```

If the GNS space is one-dimensional, the infimum over the empty orthogonal
complement and the empty Rayleigh set is, by convention, `+infinity`.

The orthogonal complement reduces `H` because `H Omega=0`. Equations
(3.9), (4.13), and the variational characterization of the bottom of a
closed nonnegative form give

```text
Delta_omega^P
 =inf_(A in D_bl, Var_omega(A)>0)
   {-i hbar omega(A^*delta_H(A))/Var_omega(A)}.            (5.3)
```

For any declared `Delta>0`, the following are therefore equivalent:

1. `H restricted to Omega^perp>=Delta`;
2. for every `A in D_bl`,

   ```text
   -i hbar omega(A^*delta_H(A))
    >=Delta Var_omega(A).                                 (5.4)
   ```

If (5.4) holds and `eta in ker(H) intersect Omega^perp`, then

```text
0=<eta,H eta> >=Delta||eta||^2,                           (5.5)
```

after form-core approximation. Hence `eta=0` and

```text
ker(H)=C Omega.                                           (5.6)
```

Thus a positive value in (5.3) proves both the simple ground vector and the
state-vector GNS spectral gap. Neither conclusion is proved merely by writing
the exact formula.

## 6. Parity-twisted unitary equivalence and equality of the two gaps

Restore the signs. On the dense cyclic subspace define

```text
W_gamma pi_-(A)Omega_-:=pi_+(gamma(A))Omega_+.            (6.1)
```

Equation (2.5) gives

```text
||pi_+(gamma(A))Omega_+||^2
 =omega_+(gamma(A^*A))
 =omega_-(A^*A).                                         (6.2)
```

Thus (6.1) is isometric. Since `gamma` is onto, its range is dense, and it
extends to a unitary from the minus GNS space to the plus GNS space. Moreover,

```text
W_gamma U_-(t)pi_-(A)Omega_-
 =pi_+(gamma(theta_t(A)))Omega_+
 =pi_+(theta_t(gamma(A)))Omega_+
 =U_+(t)W_gamma pi_-(A)Omega_-.                           (6.3)
```

Stone uniqueness then gives

```text
W_gamma H_- W_gamma^*=H_+.                               (6.4)
```

Consequently the implementing spectra, zero-kernel multiplicities, and
Poincare constants agree:

```text
Delta_-^P=Delta_+^P.                                     (6.5)
```

This is equivalence after twisting the observable label by `gamma`. It is not
an untwisted quasi-equivalence theorem and does not decide whether the two
states are pure, factorial or disjoint.

## 7. Exact mesoscopic-source full-gap collapse

The full finite-volume source Hamiltonians have compact resolvent and unique
grounds. Choose the strictly positive phases used by v4.0 and put

```text
c_L:=<phi_L^+,phi_L^-> in (0,1].                         (7.1)
```

For two normalized vector states, the exact trace-norm formula is

```text
|| |phi_L^+><phi_L^+|-|phi_L^-><phi_L^-| ||_1
 =2 sqrt(1-c_L^2).                                       (7.2)
```

Apply both states to the same contraction `b` from (2.10). Their expectation
difference is at least `d`, so (7.2) gives, eventually,

```text
d<=2 sqrt(1-c_L^2),
1-c_L^2>=d^2/4.                                          (7.3)
```

For the plus-source Hamiltonian define the normalized orthogonal vector

```text
zeta_L^+
 :=(phi_L^--c_L phi_L^+)/sqrt(1-c_L^2),
<phi_L^+,zeta_L^+>=0.                                    (7.4)
```

The zero-source and both source forms share one domain. The exact parity
relations are

```text
E_L(-h_L)=E_L(+h_L)=:E_L(h_L),
s_L^-=-s_L^+,
H_L(+h_L)=H_L(-h_L)-2h_L S_L.                            (7.5)
```

Since `phi_L^-` is the exact ground of `H_L(-h_L)`, (7.5) yields

```text
q_(H_L(+h_L)-E_L(h_L))[phi_L^-]
 =-2h_L s_L^-
 =2h_L s_L^+.                                            (7.6)
```

The cross terms with `phi_L^+` vanish in the closed quadratic form because it
is the exact plus ground. Equations (7.4)--(7.6) therefore give

```text
q_(H_L(+h_L)-E_L(h_L))[zeta_L^+]
 =2h_L s_L^+/(1-c_L^2).                                  (7.7)
```

The form min-max principle, (2.7), and (7.3) imply

```text
Delta_(L,+)^full
 <=2h_L s_L^+/(1-c_L^2)
 <=8 C_S h_L V/d^2.                                      (7.8)
```

Parity gives the identical bound for the minus source. Substitute

```text
C_S=sqrt(B_a),
d=r_w sqrt(rho_*)/2,
d^2=r_w^2 rho_*/4,                                       (7.9)
```

to obtain the exact derived coefficient

```text
Delta_(L,sigma)^full
 <=[32 sqrt(B_a)/(r_w^2 rho_*)]h_L V ->0.                (7.10)
```

For the canonical v4.0 choice `h_L=h_*V^(-3/2)`, this is

```text
Delta_(L,sigma)^full
 <=[32 sqrt(B_a)h_*/(r_w^2 rho_*)]V^(-1/2).              (7.11)
```

No number in (7.10)--(7.11) is pasted: the coefficient follows from the
source-uniform collective bound and the fixed-witness separation constant.

## 8. Why the full-gap collapse is not a phasewise gap no-go

At each `L`, `zeta_L^sigma` is a global vector built from the two opposite
finite-volume source grounds. It changes with `L` and implements a parity-
branch switch. Nothing in the v4.0 categorical construction identifies this
sequence with

```text
pi_sigma(A)Omega_sigma-omega_sigma(A)Omega_sigma          (8.1)
```

for one fixed `A in D_bl`.

The full finite gap takes an infimum over all finite-volume directions before
the limit. The target Poincare constant takes the limit of each fixed carrier
direction and only then takes the core infimum. In general,

```text
lim_L inf_(finite directions) q_L/||.||^2
 !=inf_(fixed A in D_bl) lim_L q_L(A)/Var_L(A).           (8.2)
```

No interchange is permitted. Equation (7.10) rules out only the attempted
v3.0 input

```text
liminf_L Delta_(L,sigma)^full>0.                          (8.3)
```

It does not establish `Delta_sigma^P=0`. This exact scope is the content of
the new negative result.

## 9. Fixed-carrier finite-source coercivity is equivalent to the target gap

For every fixed `A in D_bl`, weak-star convergence gives

```text
omega_L^sigma(A^*delta_H(A))
 ->omega_sigma(A^*delta_H(A)),                            (9.1)
```

because both factors are fixed elements of `A_H^0`. Equation (2.9) then turns
(2.8) into

```text
cal E_L^sigma(A)
 ->-i hbar omega_sigma(A^*delta_H(A)).                    (9.2)
```

Similarly,

```text
Var_(omega_L^sigma)(A)->Var_(omega_sigma)(A).             (9.3)
```

Combining (5.3), (9.2), and (9.3) proves the exact equivalence: for every
fixed `Delta>0`,

```text
H_sigma restricted to Omega_sigma^perp>=Delta             (9.4)
```

if and only if, for every fixed `A in D_bl`,

```text
lim_L[
 cal E_L^sigma(A)
 -Delta Var_(omega_L^sigma)(A)
]>=0.                                                     (9.5)
```

Parity reduces (9.5) to one sign. The quantifiers are load-bearing: the same
`Delta` must work for every fixed core element. Individual positivity of
`cal E_L^sigma(A)` is only groundness and supplies no uniform Rayleigh floor.

Equation (9.5), rather than a uniform full finite-volume gap, is the honest
remaining finite-to-target route. An alternative may identify these states
with a cutoff-stable QPS/Yarotskii branch carrying an independently proved
uniform phase gap. Neither input is established here.

## 10. Nonduplication

### 10.1 EXP-000789

EXP-000789 works at exactly zero source. Its odd collective trial
`S_L Omega_L/||S_L Omega_L||` gives

```text
Delta_L^full<=hbar^2/(2 chi V m_L^2)=O(V^(-1)).           (10.1)
```

The present equation (7.10) concerns unique nonzero mesoscopic-source grounds.
It uses one fixed categorical witness to prevent their overlap from tending to
one, then uses the opposite exact source ground as the branch-switching trial.

### 10.2 EXP-000790 and R-167 v1.8

EXP-000790 states the abstract broken-sector Poincare test. R-167 v1.8 proves
its equivalence with an OS temporal-mass bound after assuming a centered form
core and simple zero-energy kernel. Neither proves the centered form-core
property for the actual v4.0 categorical system. Sections 3--5 do.

### 10.3 R-167 v3.0

The v3.0 transfer theorem assumes:

1. a positive uniform finite Poincare inequality;
2. convergence to one prescribed target generator;
3. the target energy identity;
4. a centered target form core.

V4.0 supplied an exact target generator on `D_bl` and a vanishing represented
residual rather than a norm-bounded source commutator. Sections 3--4 discharge
the target energy and form-core premises. Section 7 proves that the full
finite-source gaps cannot discharge premise 1. The fixed-carrier inequality
(9.5) remains open.

### 10.4 R-167 v3.4

V3.4 gives a conditional Yarotskii phasewise gap at each fixed Ritz label only
after separately assuming a theorem rectangle. It supplies no `M`-uniform
cutoff passage and no identification of those branches with `omega_+/-` on
`A_H^0`. The present theorem neither imports nor upgrades that conditional
result.

### 10.5 R-167 v4.0

V4.0 constructs the ground pair and the bandlimited algebra graph core. It
explicitly leaves the represented centered GNS form core, simple kernel and
positive Poincare inequality open. The present result closes the form-core and
exact reduction only. It proves no positive value for (5.3).

## 11. Devil's-advocate audit

### Objection 1: a graph core for delta_H need not be a form core for H^(1/2)

**DISMISSED HERE BY A SEPARATE ARGUMENT.** The conclusion is not inferred from
the words graph core. Section 4 uses cyclic density, compact implementing-
energy multipliers, boundedness of `H^(1/2)` on each filtered range, centering,
and a two-stage diagonal approximation.

### Objection 2: the proof assumes the implementing kernel is already simple

**DISMISSED.** Section 4 works on all of `Omega^perp`, including any additional
zero vectors. Only a subsequently proved positive Poincare inequality would
force those vectors to vanish.

### Objection 3: parity-related states have the same representation without a twist

**UPHELD AS A FIREWALL.** Equation (6.1) intertwines `pi_-(A)` with
`pi_+(gamma(A))`, not necessarily with `pi_+(A)`. The theorem is parity-
twisted unitary equivalence and makes no untwisted quasi-equivalence claim.

### Objection 4: a vanishing full source gap proves the phase GNS is gapless

**UPHELD AS A FIREWALL.** The upper-bound trial is global and `L`-dependent.
It may leave the selected phase representation in the thermodynamic limit.
Section 8 forbids interchanging the finite infimum and fixed-carrier limit.

### Objection 5: the trace-distance overlap bound has the wrong factor

**DISMISSED.** For normalized pure vectors the trace norm of the projector
difference is exactly `2 sqrt(1-|c_L|^2)`. A contraction expectation split
`d` therefore yields `1-|c_L|^2>=d^2/4`, which produces the factor `8` in
(7.8) and the derived factor `32` in (7.10).

### Objection 6: v4.0 residual closure already proves coercivity

**UPHELD AS A FIREWALL.** Residual closure passes the nonnegative source form
to target ground positivity one fixed element at a time. It supplies no common
strict lower bound relative to variance. Groundness is not a spectral gap.

### Objection 7: the new negative duplicates the zero-source full-gap record

**DISMISSED.** The older record is the `h=0` odd-trial `O(1/V)` theorem. The
new record tests the proposed use of unique `h_L>0` source-ground gaps and
derives `O(h_LV)` through overlap control from the fixed categorical witness.
Its failure mode and proof input are different.

## 12. Scope, lifecycle and reproduction

This result is T0 and `claim_bearing:false`. It proves no positive phasewise
Poincare constant, simple GNS kernel, broken-sector GNS gap, physical mass gap,
purity, factoriality, disjointness or phase exhaustion. It does not identify
the diagonal v4.0 clusters with the EXP-000790 iterated fixed-source tangents,
EXP-000781 finite-beta DLR tangents, v3.8 KMS states, or v3.4 fixed-Ritz
Yarotskii branches. It proves no beta-infinity KMS limit, DLR/OS
identification, spatial all-exhaustion or raw quasi-local oscillator common
alpha, regulator removal, continuum, physical vacuum or empty-space
comparison, Round-1, C6, CP1, physical Sector A or Pre-A closure.

Both historical gates and all five active parent gates remain OPEN. The new
negative result is scoped to automatic uniform-Poincare transfer from the full
mesoscopic-source gaps. The ordered-ground-doublets negative is reused for the
broader abstract nonimplication.

Proof-first staged reproduction:

```text
E:\Dev\TECT.venv\Scripts\python.exe -X utf8 codes/foundations/pre_a_cp1_st8_q3lock_categorical_ground_bandlimited_gns_poincare_route_split.py --staged --no-store
E:\Dev\TECT.venv\Scripts\python.exe -X utf8 codes/foundations/pre_a_cp1_st8_q3lock_categorical_ground_bandlimited_gns_poincare_route_split_independent.py --staged --no-store
E:\Dev\TECT.venv\Scripts\python.exe -X utf8 codes/foundations/pre_a_cp1_st8_q3lock_categorical_ground_bandlimited_gns_poincare_route_split_verify.py --staged --no-store
```

No v4.1 PDF is issued. The next logical gate-level checkpoint will decide
whether a synthesis note is warranted.
