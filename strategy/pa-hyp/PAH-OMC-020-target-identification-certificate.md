# PAH-OMC-020 target-process identification contract

## Question and fixed scope

T-078 asks whether the anchored finite correlations in the unchanged
PAH-OMC-020 programme can be identified with the exact R-512 minimal-form
semigroup.  For a bounded local cylinder pair and a finite external Markov
time horizon, the remaining defect is

```text
D_n(f,g;T) = sup_{0 <= t <= T}
             | c_n(f,g;t) - <f, T_min(t) g>_(nu_infty) |.
```

The PAH-001 functional, original directed PH/LK/AP/TR rates, labelled Gibbs
normalisation, OMC-004 strip, external stochastic Markov time and the
`j`-then-anchored-`n` order are hash-pinned.  No new map, process, state,
carrier, regulator, rate or limit order is introduced.

## Two non-equivalent discharge routes

The target form is already a registered object: R-512 and its conditional
R-530 follow-up establish the R-512 minimal closed form and its spectral
Dirichlet/Markov properties.  That fact does not identify the finite process.
The contract therefore accepts exactly one of two complete routes.

1. **Form route.**  A source-authorized common-Hilbert comparison `U_n` must
   give cylinder recovery and norm compatibility.  The same packet must prove
   the arbitrary-sequence form liminf, a recovery sequence for every local
   target vector, exact equality with the R-512 minimal form (not a maximal
   extension), and compact-time local-correlation transfer.
2. **Path route.**  A source-authorized common path space and filtration must
   provide tight finite laws and local uniform integrability, convergence of
   the limiting martingale problem or form to the exact R-512 generator/form,
   uniqueness on the local cylinder core, and compact-time stationary
   correlation transfer.

The route is admitted only if all five fields in one route are versioned,
source-authorized, hash-pinned and independently verified.  A named target,
static Gibbs-state convergence, local generator equality, or the R-512
Dirichlet property alone does not supply `D_n`.

## Finding

The current pinned records provide the target form but neither route.  R-530
explicitly leaves the varying-space comparison, N2b liminf/recovery, N2d
identification and path construction open.  R-533 finds no source-authorized
path law, filtration, non-explosion/uniqueness packet or R-512 process
identification.  R-534 names `D_n` in the exact three-term compact-time
triangle, and R-535 marks the target-process link absent.  R-529 independently
shows why static L2 coupling cannot be promoted to dynamic form compatibility;
its two-state example is only an abstract insufficiency diagnostic.

The primary replay passes 42/42 checks, the non-importing independent replay
passes 33/33, the hostile mutation suite passes 21/21, and the integrated
replay passes 23/23.  Lean 4.32.1 compiles eight finite Boolean route
obligations.  The result is therefore `HOLD_FOR_EVIDENCE` and
`auxiliary_support`: `D_n` remains uninstantiated.

## Remaining evidence contract

Provide one versioned source-authorized packet completing either the five
form-route fields or the five path-route fields, with the declared compact
time and anchored-volume quantifiers, terminal-square convention and original
normalisation.  Then rerun the unchanged admission predicate once.  An exact
PAH-specific contradiction to a registered local correlation would instead
open the negative-result branch.  Do not repeat finite carriers, static
coupling fixtures, physical-empty, BCC or Reading-H calculations.

## Non-claims

This checkpoint does not prove PAH-OMC-020 anchored-`n` convergence, a path
process, an infinite-volume jump process, minimal/maximal domain equality,
spectral gap, ergodicity, physical Pre-A, spacetime, QFT, gravity, continuum,
mass gap, Yang--Mills or TOE.  The external Markov parameter is not quantum
real time, proper time or Lorentzian time.
