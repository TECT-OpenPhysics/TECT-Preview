# R-427 certificate -- Reading-H physical-empty bounded test revalidation (blocked)

## 1. Result and exact scope

R-427 is an append-only T0, claim-nonbearing TECT-owner revalidation under
EXP-001272.  It repeats the R-420 preregistration with the same comparison

```text
F_total[G_*] - F_total[E]
```

and the same fifteen-field common-owner contract.  The field names, candidate,
orientation and admission firewall are identity-locked to the R-420 manifest;
the values are not invented when the current owner does not supply them.

The three requested outputs are the sign of the total-energy difference, full
regulated Reading-H stationarity and symmetry-projected transverse stability.
The revalidation is blocked before any numerical energy, derivative or Hessian
is evaluated because no common finite parent and no admitted physical-empty
branch are present in the cited authorities.

## 2. Frozen contract and E preregistration

The frozen fields are: finite-regulator functional, reference definition,
normalization, finite-part counterterm scheme, regulator and cutoff, physical
volume, boundary condition, thermodynamic-then-continuum limit order,
representations of `G_*` and `E`, full admissible tangent, symmetry group and
orbit tangent, transverse projector, Hessian/error budget, and stationarity
residual budget.  R-427 records these as the same fifteen-field identity as
R-420, while its value status remains `UNSUPPLIED_BY_CURRENT_OWNER`.

`E` is preregistered as a slot for a normalized constrained, no-condensate or
empty-preparation branch in the same parent as `G_*`.  It is not admitted and
has no supplied state/configuration, measure/algebra, normalization, boundary
preparation, physical interpretation or local criterion.  Zero, the P1
zero/disordered reference, the Gaussian reference and an equilibrium phase are
not aliases for `E`.

## 3. Executed verdicts

| Test | Verdict | Reason |
|---|---|---|
| `F_total[G_*]-F_total[E]` sign | `BLOCKED_NOT_EVALUATED` | no common finite-regulator owner, no admitted `E`, and no absolute physical-empty normalization |
| Full Reading-H stationarity | `BLOCKED_NOT_EVALUATED` | no full regulated tangent or stationarity residual budget; a covariance-owner stationarity statement is insufficient |
| Symmetry-projected transverse stability | `BLOCKED_NOT_EVALUATED` | no common orbit tangent, transverse projector, regulated Hessian or error budget |

These are typed input-boundary statuses, not zero values and not numerical sign
observations.

## 4. Evidence and adversarial review

The primary lane hash-checks the six authorities, the R-420 identity lock, all
fifteen fields and all three blocked statuses.  The non-importing independent
lane reconstructs the same predicates and authority hashes.  The hostile lane
rejects eight mutations: P1/zero aliasing, admitting E without data, marking
missing finite parts fixed, switching to a P1 owner, forging a numeric sign,
omitting the limit order, forging a top-level PASS, and breaking the R-420
contract identity.  The integrated lane checks the outputs and compiles Lean
R427.  Lean proves only that evaluation requires an owner and an admitted E and
that the three blocked predicates cannot be reported as evaluated.

Evidence level: **T0 / EXECUTED APPEND-ONLY PREREGISTRATION REVALIDATION AND
INPUT-ADMISSIBILITY AUDIT**.  No energy, stationarity residual or Hessian was
computed.

## 5. Assumptions and missing assumptions

Assumptions:

1. The six listed authorities are the active TECT owner boundaries.
2. The sign orientation is `G_*` minus `E`, and all tests require one common
   regulated parent.
3. The fifteen R-420 fields are identity-locked; locking their names does not
   supply absent owner values.
4. A physical-empty branch needs a normalized representative, measure or
   algebra, boundary preparation and a local physical criterion beyond a
   one-point zero condition.
5. A preregistration audit may refuse undefined evaluation; refusal is not a
   sign or stability theorem.
6. Transverse means orthogonal to the exact declared symmetry-orbit tangent.

Missing assumptions are a hash-pinned finite `F_total` containing both entries,
an admitted normalized `E` with a no-condensate or clustering criterion, common
reference/normalization/finite parts and renormalization conditions, explicit
regulator/volume/boundary/limit data, deterministic maps for both entries, a
full tangent and orbit tangent, a transverse projector, and stationarity and
Hessian error budgets.

## 6. Boundary and next unlock

R-427 changes no claim tier, adds no negative result and issues no PDF.  It does
not identify physical empty space, determine a below-empty sign, prove
Reading-H stationarity or transverse stability, or advance C6, Sector A,
Pre-A, Yang--Mills or mass-gap claims.  The next unlock is one owner-approved
hash-pinned finite parent plus a normalized and physically justified `E`; both
entries, the full tangent/projector, finite parts and error budgets must then be
frozen before rerunning the unchanged three tests.

