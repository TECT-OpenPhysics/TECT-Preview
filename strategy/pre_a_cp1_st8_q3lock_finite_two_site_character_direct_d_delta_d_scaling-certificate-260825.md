# EXP-001086 — finite two-site local-character direct (D,delta-D) scaling audit

## QFT-facing scope and reproduction

This claim-nonbearing T-054 package tests the smallest nontrivial local
observable whose support is enlarged beyond the single-site character used in
EXP-001085:

```text
A_2 = exp(i * character_amplitude * (q_0 + q_1) / hbar)
D_sigma(t) = tau_(H+sigma W_L)(t)(A_2) - tau_H(t)(A_2)
delta D_sigma = [-beta H, D_sigma]
```

The finite reference state is the full uncut Q3 Gibbs state. The smooth cosine
cutoff is applied only to the bond coordinates, so `W_L=H-H_L` tests the
declared source perturbation without changing the onsite terms. Both
orientations `sigma=+1` and `sigma=-1` are retained, and the two-sided Gibbs
seminorm is `Tr(rho X*X)+Tr(rho XX*)`.

```text
python -X utf8 codes/foundations/pre_a_cp1_st8_q3lock_finite_two_site_character_direct_d_delta_d_scaling.py --self-test
python -X utf8 codes/foundations/pre_a_cp1_st8_q3lock_finite_two_site_character_direct_d_delta_d_scaling_independent.py --self-test
python -X utf8 codes/foundations/pre_a_cp1_st8_q3lock_finite_two_site_character_direct_d_delta_d_scaling_verify.py
```

Primary: 46/46. Independent: 46/46. Integrated: 56/56 with Lean R268.

## Findings

At radius `L=0.5` and time `t=0.10`, the two-orientation sums are:

| volume | support | `D_norm` sum | `delta_D_norm` sum |
|---:|---|---:|---:|
| 2 | sites 0,1 | 0.0093767225 | 0.0213010043 |
| 4 face | sites 0,1 | 0.0168376850 | 0.0729462141 |

At `L=1.0`, the corresponding sums are `0.0024376321` and `0.0055408095`
for volume two, versus `0.0043922819` and `0.0190361869` for the four-site
face. At `L=2.0` the bond tail and direct norms are at the numerical floor.
The independent lane agrees with every primary orientation, time, radius, and
summary value within the registered relative tolerance. The largest-volume to
target ratios of the recorded maxima are `1.7956898` for `D_norm` and
`3.4245434` for `delta_D_norm`.

This is the QFT bridge being tested: a fixed-support local observable is
evolved in finite Gibbs representations while the source cutoff is removed.
The data supports finite-member cutoff decay and exposes the volume dependence
that a thermodynamic local algebra or common KMS dynamics would have to
control. It does not supply that uniform control.

## Decision and next action

`EXP-001086` advances the direct projected route only as a finite two-site
support-scaling diagnostic. The larger local observable does not remove the
modular-derivative growth seen in EXP-001085; it strengthens the reason to keep
the volume/source-uniform direct `D,delta-D` gate open. The next proof action is
to derive an analytic local-class bound with an explicit volume-independent
multiplier, or to run a larger structured support/cutoff family and register a
formal scaling obstruction if the growth persists. No common-alpha, OS/KMS/GNS,
mass-gap, continuum, C6, Sector A, or Pre-A promotion is permitted from this
finite result.

## Devil's-advocate review

1. **Observable support:** `A_2` is generated by `q_0+q_1` and is supported on
   the first two sites in both volumes. **UPHELD.**
2. **Cutoff scope:** onsite terms remain uncut; only bond coordinates are
   replaced by the smooth cosine cutoff. **UPHELD.**
3. **Reference state:** both lanes use the uncut full-volume Gibbs state rather
   than a volume-dependent local-state substitution. **UPHELD.**
4. **Orientations:** `H+W_L` and `H-W_L` are evolved separately; no parity
   identification is used. **UPHELD.**
5. **Modular derivative:** `delta D` is computed as the explicit finite Gibbs
   commutator with `-beta H`, not inferred from the size of `D`. **UPHELD.**
6. **Scaling statement:** the V=4/V=2 ratios are finite diagnostics only; they
   are not asserted as a monotone theorem or a thermodynamic lower bound.
   **UPHELD.**
7. **Independence:** the second lane reconstructs oscillator, Hamiltonian,
   cutoff, Gibbs state, character, and unitary evolution without importing the
   primary module. **UPHELD.**
8. **Lean:** R268 checks rational support/orientation/zero-tail fixtures and
   scope labels; it does not prove the floating-point spectral values or a QFT
   theorem. **UPHELD.**
9. **QFT promotion:** volume/source-uniform direct Cauchy, product/core density,
   exhaustion, group law, common alpha, OS/KMS/GNS identification, gap,
   continuum, C6, Sector A, and Pre-A remain open. **UPHELD.**

