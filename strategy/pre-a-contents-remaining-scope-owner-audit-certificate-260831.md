# R-460 certificate: Canonical Contents remaining-scope owner audit

## Identity and route role

- Result: `R-460`
- Result card: `PA-CONTENTS-REMAINING-SCOPE-OWNER-AUDIT-v0`
- Exploration: `EXP-001333`
- Task: `T-054`
- Claim bearing: `false`; tier: `T0`
- Status: `NO_STANDALONE_OWNER_FOUND_REMAINING_SCOPE`
- Route role: additive source-applicability intake. Existing T-054, T-059,
  T-061 methods, the R-192 owner order, and all promotion firewalls remain
  unchanged.

## Exact scope and method

The audit is read-only over the operator-configured canonical `Contents` root.
It scans the complementary residual scope outside R-459: `Backup`, `Codes`,
`Runs`, `Website`, and the remaining `Docs` and `Github` areas. The subtrees
`Docs/math`, `Github/note`, `Docs/status`, and `Github/status` are explicitly
excluded because R-459 already audited them. Only `.txt`, `.md`, and `.tex`
files are decoded for search; binaries and unsupported extensions are excluded.
Path counts and raw-content SHA-256 unique counts are retained, so residual
mirrors are not independent authorities.

The unchanged strict alias contract requires all five R-192 owner-slot groups:

1. generator, transfer-operator, or Markov-semigroup vocabulary;
2. heat-root, heat-kernel, or heat-semigroup vocabulary;
3. filtration, conditional-replica, or replica vocabulary;
4. raw-current, current-spatial, or spatial-intertwiner vocabulary; and
5. a production q-ledger phrase (`q-ledger`, `q_k ledger`, `one-use q`, or
   `nonnegative q ledger`).

Exact R-192 owner tokens are checked separately. A broad `one uses ... q`
phrase is a false-positive control only and cannot satisfy the strict q-ledger
group. A merged historical bundle is not an owner because it has no single
source edition, field boundary, or authority identity.

## Execution and finding

Primary, non-importing independent, integrated, and hostile lanes were run
against the same configured root. The residual scan covered **284 paths**,
**273 content-SHA-256 values**, and **11 duplicate paths**. It found:

- strict complete semantic paths: `0`;
- standalone complete owner candidates: `0`;
- exact R-192 owner-token paths: `0`;
- partial paths containing at least three groups: `3`;
- loose-q false-positive control rows: `0`;
- primary/independent summary agreement: `PASS`;
- hostile mutation firewall: `8/8` rejected;
- integrated assertions: `17/17` passed.

Therefore the bounded result is `PASS_BOUNDED_NO_STANDALONE_OWNER`: no
standalone source owner was found in this residual scope under the same alias
set. This is a complementary provenance boundary and false-positive control,
not a universal absence theorem.

The reproducible artefacts are the primary run
`claims/C6-SPACETIME-SIGNATURE/runs/2026-08-31-primary-pre_a_contents_remaining_scope_owner_audit/result.json`,
the independent run at
`claims/C6-SPACETIME-SIGNATURE/runs/2026-08-31-independent-pre_a_contents_remaining_scope_owner_audit/result.json`,
the hostile run at
`claims/C6-SPACETIME-SIGNATURE/runs/2026-08-31-hostile-pre_a_contents_remaining_scope_owner_audit/result.json`,
and the integrated assertions at
`claims/C6-SPACETIME-SIGNATURE/runs/2026-08-31-integrated-pre_a_contents_remaining_scope_owner_audit/integrated.json`.

## Assumptions and missing assumptions

Assumptions are that the configured `Contents` root is the maintained
canonical source tree, that the five groups are the same bounded aliases used
by R-459 and R-192, that the R-459 subtrees are excluded for complementarity,
that replacement decoding is search-only, and that a future owner may use
terminology outside this alias set.

Missing are a hash-pinned A1 production generator and mobility with heat-root
incidence, root-labelled filtration and conditional replicas, a raw-current
spatial intertwiner, a once-owned nonnegative production q-ledger, a common
state and physical projection, boundary/normalization/regulator/volume/limit
order, an admitted Q3 common domain with uniform majorant, and the downstream
OS/KMS/GNS or physical-sector bridge.

## Adversarial disposition

- **Scope complement:** dismissed; explicit included roots and R-459 subtree
  exclusions are asserted.
- **Merged bundle as owner:** rejected; merged rows are discovery-only.
- **Partial status as owner:** rejected; three-group rows remain references.
- **Loose q phrase:** dismissed; strict q-ledger aliases are unchanged.
- **Terminology coverage:** open; genuinely different vocabulary is not
  refuted by this bounded scan.
- **Path mirrors:** dismissed as a source of support; raw SHA-256 controls
  duplicate paths.
- **Method overhaul:** dismissed; this is additive provenance intake only.
- **Promotion leakage:** rejected; the result remains T0 and
  non-claim-bearing.

## Boundary and next gate

R-460 supplies no generator, heat semigroup, filtration, current map, q-ledger,
candidate, observation map, score, physical identity, continuum,
QFT/Yang--Mills, Pre-A, Sector-A, cosmic-origin, theory-of-everything, or
mass-gap conclusion. It does not replace or revise the established research
methods.

Keep the forward owner branch parked under the existing R-192 fingerprint and
treat R-459 and R-460 as complementary bounded source audits. Reopen the
owner branch only if a new standalone, hash-pinned source supplies the complete
owner fields. Continue the unchanged T-054 Q3LOCK owner intake and T-059/T-061
observation-source lock. Do not create another finite mobility, physical-empty,
or geometry table in response to this source boundary.
