# runs/ — evidence artefacts

`runs/<claim-id>/<YYMMDD>-<tag>/result.json` — config, seeds, environment,
per-assert pass/fail with expected/actual, summary. Large binaries are
git-ignored; JSON + generating script must suffice to reproduce. A run cited
as evidence without its JSON artefact is not citable.
