# CLAUDE.md — compatibility pointer

The single binding AI-collaborator protocol for this repository is
[`AGENTS.md`](AGENTS.md). Read and follow it in full.

This file exists only for clients that automatically discover `CLAUDE.md`.
It deliberately contains no duplicate workflow, commit, verification, or
publication rules; duplicating them previously allowed the two protocols to
drift.
